## 2026-02-14 04:49 — P5-T6 | COMPLETE

Manual Phase 5 verification accepted by user review; Phase 5 marked complete.
Confirmed monthly/annual toggle, asset column toggle with contained horizontal scroll, sticky headers, sortable columns, virtualization behavior, and rerun update flow.

## 2026-02-14 04:37 — P5-T6 | IN PROGRESS

Phase 5 automated verification passed with no regressions: `npm run build`, `npm run typecheck`, `npm run lint`, and `npm test` all succeed (server tests 25/25).
Manual visual DoD pass for table interactions remains pending user confirmation.

## 2026-02-14 04:35 — P5-T5 | COMPLETE

Integrated `DetailTable` into the output stack below chart and summary components; table updates from cached simulation results only.
Input edits without rerun preserve existing table data, while reruns refresh rows for the current mode dataset.

## 2026-02-14 04:34 — P5-T4 | COMPLETE

Implemented monthly-view row virtualization with overscan and spacer rows for scalable rendering up to full 480-month horizons.
Sticky control/header behavior is preserved while scrolling within the table container.

## 2026-02-14 04:32 — P5-T3 | COMPLETE

Added sortable headers across table columns with asc/desc/off cycle and consistent number formatting for currency and percentage cells.
Sorting is applied to both monthly and annual datasets via shared table helpers.

## 2026-02-14 04:31 — P5-T2 | COMPLETE

Implemented Detail Table UI with Monthly/Annual toggle (#49), Asset Class Columns toggle (#50), sticky controls/header (#51), and empty state before first run.
Added output table layout and control wiring in the output panel.

## 2026-02-14 04:30 — P5-T1 | COMPLETE

Extended store with table view actions (`setTableGranularity`, `setTableAssetColumnsEnabled`, `setTableSort`) and added row transformation/sorting helpers for monthly + annual data.
Implemented annual aggregation and table-ready row projection from simulation output rows.

## 2026-02-14 04:28 — SESSION START | Resuming from Phase 5, Task P5-T1. Last completed: P4-T6 (Phase 4 complete).

Read startup state and Phase 5 requirements from `docs/ENGINEERING.md` and `docs/SPECS.md` before implementation.

## 2026-02-14 04:14 — P4-T6 | COMPLETE

Manual Phase 4 visual verification accepted by user review; Phase 4 marked complete.
Output chart/stats affordances (#33-#40, #42-#44, #47-#48) validated with rerun behavior and stochastic manual-run flow.

## 2026-02-14 04:01 — P4-T6 | IN PROGRESS

Phase 4 automated verification passes: `npm run build`, `npm run typecheck`, `npm run lint`, and `npm test` all succeed with server Phase 2 suite still green (22/22).
Manual visual DoD pass for chart/stats interactions remains pending user confirmation.

## 2026-02-14 04:00 — P4-T5 | COMPLETE

Integrated `SummaryStatsBar` and `PortfolioChart` into `AppShell` output area with idle-state rendering and cached-result display behavior.
Input edits now leave prior run outputs unchanged until Run Simulation executes again, preserving Phase 4 rerun semantics.

## 2026-02-14 03:59 — P4-T4 | COMPLETE

Added chart hover crosshair/tooltip (period, portfolio value, withdrawal), zoom range sliders, and Reset Zoom control in `PortfolioChart`.
Zoom state is persisted in store (`ui.chartZoom`) and applies only to rendered run results.

## 2026-02-14 03:57 — P4-T3 | COMPLETE

Implemented output chart rendering for manual single-path runs with total line view and asset-class stacked area toggle.
Added Real/Nominal display mode switching that recalculates y-axis scale and plotted series from inflation-adjusted values.

## 2026-02-14 03:55 — P4-T2 | COMPLETE

Implemented `SummaryStatsBar` + `StatCard` components for affordances #33–#40 with idle placeholders, computed values, and terminal-value success/failure styling.
Probability of Success card (#41) remains intentionally hidden until Monte Carlo support in Phase 8.

## 2026-02-14 03:53 — P4-T1 | COMPLETE

Added Phase 4 output helpers: active result selector, formatting utilities, and summary statistics computations (nominal + real withdrawal metrics).
Statistics derive directly from cached `MonthlySimulationRow[]` to keep output updates tied to simulation runs only.

## 2026-02-14 03:50 — SESSION START | Resuming from Phase 4, Task P4-T1. Last completed: P3-T6 (Phase 3 complete and pushed).

Read startup context (`PROGRESS.txt`, `TASKS.md`) and pulled Phase 4 requirements from `docs/ENGINEERING.md`, `docs/SPECS.md`, `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/API.md`, `docs/PRD.md`, and `docs/SCENARIOS.md` before implementation.

## 2026-02-14 03:42 — P3-T6 | COMPLETE

Manual Phase 3 verification accepted by user review; Phase 3 marked complete.
Confirmed sidebar input round-trip behavior, collapsible interactions, and run-simulation cache write path are ready for Phase 4 handoff.

## 2026-02-14 03:34 — P3-T5 | COMPLETE

Phase 3 regression and static checks passed: `npm run build`, `npm run typecheck`, `npm run lint`, and `npm test` all succeed after client input-panel wiring.
Phase 2 engine/route tests remain green (22/22 passing).

## 2026-02-14 03:32 — P3-T4 | COMPLETE

Wired command bar mode toggles to Zustand store and integrated Run Simulation API call to `/api/v1/simulate`, caching results in store without rendering output views.
Implemented strategy selector with all 12 names; only Constant Dollar parameters are active and others show "coming in Phase 6".
[ASSUMPTION] To keep requests valid before Phase 6/7 implementation, run payload generation coerces unsupported strategy/drawdown selections to Constant Dollar + Bucket.

## 2026-02-14 03:30 — P3-T3 | COMPLETE

Implemented sidebar input sections for Core Parameters, Starting Portfolio, Return Assumptions, Spending Phases, Withdrawal Strategy, Drawdown Strategy, Income Events, and Expense Events.
All controls are wired as controlled inputs to Zustand state and re-render from store values; Return Assumptions visibility toggles by simulation mode.

## 2026-02-14 03:28 — P3-T2 | COMPLETE

Added shared input/display primitives required by Phase 3: NumericInput, CurrencyInput, PercentInput, SegmentedToggle, Dropdown, MonthYearPicker, ToggleSwitch, DonutChart, and CollapsibleSection.
Sidebar sections now use these shared controls consistently.

## 2026-02-14 03:26 — P3-T1 | COMPLETE

Expanded client store model/actions for full input wiring, including spending phase cascade logic, bucket ordering controls, income/expense event CRUD, section collapse state, and simulation cache status.
Added guardrails for phase add/remove constraints (max 4 phases; prevent deleting last phase).
## 2026-02-14 03:09 — P2-T6 | COMPLETE

Phase 2 DoD verification passed: `npm run build`, `npm run typecheck`, `npm run lint`, and `npm test` all succeed.
Validated live API call on `POST /api/v1/simulate` (server on port 3011) returning 120 monthly rows; Year 1 monthly withdrawal = `333333`, Year 2 monthly withdrawal = `343333`, and first stocks draw occurs only after cash depletion.

## 2026-02-14 03:05 — P2-T5 | COMPLETE

Added required Phase 2 test suite: strategy, drawdown, simulator, helpers, and route integration tests under `packages/server/tests`.
All required test files now pass with deterministic assertions for Year 1/Year 2 withdrawal behavior and bucket depletion order.

## 2026-02-14 03:03 — P2-T4 | COMPLETE

Implemented `simulateRetirement` month loop and `POST /api/v1/simulate` route with shared Zod validation.
Invalid request payloads now return 400 with structured field-level errors; valid payloads return `SimulateResponse` monthly rows and summary.

## 2026-02-14 03:01 — P2-T3 | COMPLETE

Implemented Constant Dollar strategy and Bucket drawdown modules in server engine.
Constant Dollar follows Year 1 portfolio-rate rule and Year 2+ inflation-adjusted prior-clamped withdrawal rule; Bucket drawdown applies sequential asset depletion with shortfall reporting.

## 2026-02-14 03:00 — P2-T2 | COMPLETE

Implemented helper modules for `roundToCents`, `pmt`, inflation utilities, and return generation scaffolding.
PMT includes zero-rate fallback and positive-rate formula aligned with the strategy appendix.

## 2026-02-14 02:58 — P2-T1 | COMPLETE

Expanded shared domain/contracts with full Phase 2 simulation configuration/result types and `SimulateRequest`/`SimulateResponse` contracts.
Added `simulateRequestSchema` (Zod) with field constraints for API validation.
## 2026-02-14 02:53 — P1-T6 | COMPLETE

Ran Phase 1 verification commands successfully: `npm run build`, `npm run typecheck`, `npm run lint`, and `npm test`.
Validated `npm run dev` startup outside sandbox (client + server started), and verified health endpoint response with `curl http://localhost:3001/api/v1/health` -> `{"status":"ok"}`.
[DECISION] Browser rendering + console log behavior is implemented and ready for user manual verification during review.

## 2026-02-14 02:50 — P1-T5 | COMPLETE

Created Zustand store scaffold matching the architecture tree in `packages/client/src/store/useAppStore.ts` with slice stubs in `packages/client/src/store/slices/*`.
Store includes mode, simulation mode, core params, portfolio, strategy slices, events, simulation results, and UI state placeholders.

## 2026-02-14 02:47 — P1-T4 | COMPLETE

Scaffolded Vite + React + Tailwind client with a command bar placeholder, sidebar/output `AppShell`, and themed color tokens aligned to SPECS palette (`#1A365D`, `#4A90D9`, `#2EAD8E`, amber cash color).
Added typed API health client and app-mount health call logging in `packages/client/src/App.tsx`.

## 2026-02-14 02:43 — P1-T3 | COMPLETE

Implemented Fastify app with CORS and structured logger config, plus `GET /api/v1/health` route returning `{ status: "ok" }`.
Added server entrypoint with configurable `PORT` and startup logging.

## 2026-02-14 02:41 — P1-T2 | COMPLETE

Created shared package structure under `packages/shared/src/{domain,contracts,ui,constants}` with placeholder `SimulationConfig` and `SimulationResult` stubs and required enums (`AssetClass`, `WithdrawalStrategyType`, `SimulationMode`).
Configured ESLint import-boundary rule preventing `domain`/`contracts` imports from `ui`; verified by temporary probe import that fails lint.

## 2026-02-14 02:39 — P1-T1 | COMPLETE

Initialized the monorepo baseline for Phase 1 with root workspace/tooling files and package scaffolding for `client`, `server`, and `shared`. Added root scripts for `dev`, `build`, `test`, `typecheck`, and `lint`.

## 2026-02-14 02:39 — SESSION START | Resuming from P1-T1. Last completed: none. Next: P1-T1 (Initialize monorepo workspace scaffolding).

Read startup context files and required architecture/spec docs for Phase 1 before editing. `PROGRESS.txt` was empty and `TASKS.md` did not exist, so Phase 1 planning started from a clean state.

## 2026-02-14 10:30 — P6-T1..P6-T5 | COMPLETE

Implemented Phase 6 end-to-end across shared/server/client.

- Expanded shared withdrawal strategy domain model to a full discriminated union with 12 strategy-specific param shapes.
- Expanded `simulateRequestSchema` to validate all strategy variants via a discriminated union.
- Added server strategy registry (`engine/strategies/index.ts`) and 11 new strategy modules: Percent of Portfolio, 1/N, VPW, Dynamic SWR, Sensible Withdrawals, 95% Rule, Guyton-Klinger, Vanguard Dynamic Spending, Endowment, Hebeler Autopilot II, CAPE-Based.
- Updated simulator annual strategy calculation to use registry context (year, retirement horizon, previous clamped withdrawal, prior-year return, remaining years, and prior-year start portfolio).
- Added strategy test coverage: one test file per strategy (12 total including existing Constant Dollar) with Year 1, multi-year, and edge checks. Guyton-Klinger includes rule-specific and worked-example sequence coverage.
- Implemented client Phase 6 strategy parameter panels and per-strategy tooltip content; removed prior Constant-Dollar coercion in `getCurrentConfig` so selected strategy is sent to server.
- Added `setWithdrawalParam` store action and full strategy default parameter state.
- Updated `TASKS.md` with Phase 6 granular tasks and marked complete.

Verification run (all passing):
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm test`

## 2026-02-14 13:15 — Post-Phase 6 polish | COMPLETE

Applied post-review UI refinements and defaults tuning before Phase 7:

- Added Detail Table `Spreadsheet Mode` toggle in output controls.
  - When enabled, table container internal scrollbars are removed.
  - Monthly virtualization is disabled so all rows render continuously (spreadsheet-style expansion).
  - Horizontal/vertical scrolling is delegated to the page/layout instead of the inner table viewport.
- Corrected default client portfolio values to dollar-scale inputs:
  - Stocks: $2,000,000
  - Bonds: $250,000
  - Cash: $50,000
- Adjusted default spending phase bounds to:
  - Min monthly: $6,000
  - Max monthly: $12,000

Verification:
- `npm run typecheck -w @finapp/client` passes.
- `npm run lint -w @finapp/client` passes.

## 2026-02-14 13:26 — Defect fix: withdrawalsStartAt ignored | COMPLETE

Fixed a withdrawal-start timing bug where engine withdrawals were being computed/clamped from Year 1 regardless of `coreParams.withdrawalsStartAt`.

Changes:
- Server simulator now computes `withdrawalStartYear = max(1, withdrawalsStartAt - startingAge)` and suppresses both strategy calculation and spending-phase clamping before that year.
- Added simulator regression test for delayed start case (`startingAge=55`, `withdrawalsStartAt=60`) validating Year 1-4 withdrawals are zero and Year 5 starts withdrawals.
- Client spending phase boundary recalculation now respects the same start-year delta so Phase 1 no longer always hard-pins to Year 1 after core param edits.

Verification:
- `npm run test -w @finapp/server` passes (including new regression test).
- `npm run typecheck -w @finapp/client` passes.
- `npm run lint -w @finapp/client` passes.

## 2026-02-14 13:31 — Defect fix: spending phase boundary pinning | COMPLETE

Fixed Spending Phase boundary behavior for timeline constraints:

- First phase `startYear` is now pinned to the withdrawals-start delta year (`withdrawalsStartAt - startingAge`, clamped to timeline).
- Last phase `endYear` is now pinned to `retirementDuration` so it always updates when retirement duration changes.
- UI now locks boundary inputs accordingly:
  - first phase `Start Year` is non-editable
  - last phase `End Year` is non-editable
  - with a single phase, both boundary fields are non-editable
- Store-side patch sanitization now ignores user attempts to mutate locked boundaries directly.

Verification:
- `npm run typecheck -w @finapp/client` passes.
- `npm run lint -w @finapp/client` passes.

## 2026-02-14 13:47 — Defect fix: phase add boundary inversion + age clamp | COMPLETE

Fixed two related client-side validation/state issues:

- Resolved Spending Phase add-path bug that could create `Start Year > End Year` (e.g., Phase 2 start 36 / end 35 after changing retirement duration and adding a phase).
  - Root cause: boundary recalculation used prior *raw* phase values instead of prior recalculated boundaries.
  - Fix: switched to sequential recalculation using accumulated prior end-year, with timeline-cap aware bounds per remaining phases.
- Enforced `Withdrawals Start At >= Starting Age`:
  - UI input minimum now tracks current `startingAge`.
  - Store mutation clamps `withdrawalsStartAt` upward if user sets it lower.
  - Updating `startingAge` now also raises `withdrawalsStartAt` if needed.
- Added add-phase capacity guard based on available timeline years so impossible boundary packing is prevented.

Verification:
- `npm run typecheck -w @finapp/client` passes.
- `npm run lint -w @finapp/client` passes.

## 2026-02-14 15:28 — Phase 7: Drawdown & Events | COMPLETE

Implemented full Phase 7 across shared contracts, server engine, client input UI, and verification.

Changes:
- Shared model/contracts (`@finapp/shared`):
  - Upgraded drawdown config to support both `bucket` and `rebalancing` strategies via discriminated union.
  - Added full event domain fields for income/expense timing and behavior:
    - `depositTo` / `sourceFrom`
    - `start`, `end` (including `"endOfRetirement"`)
    - `frequency`
    - `inflationAdjusted`
  - Expanded `simulateRequestSchema` validation:
    - Rebalancing target allocation must sum to 1 (100%).
    - Glide path allocation entries must sum to 1.
    - Glide path requires >= 2 waypoints when enabled.
    - Recurring events require `end >= start`.
- Server drawdown engine:
  - Added `engine/drawdown/rebalancing.ts` with:
    - Overweight-first withdrawal sourcing.
    - Proportional fallback drawdown across remaining assets.
    - Glide path interpolation helper by retirement year.
    - Partial-fulfillment + shortfall reporting.
  - Added drawdown registry (`engine/drawdown/index.ts`) used by simulator.
- Server simulator:
  - Replaced bucket-only sourcing with strategy-aware drawdown dispatch.
  - Added monthly income processing (timing/frequency/inflation/deposit asset).
  - Added monthly expense processing (timing/frequency/inflation/source asset/follow-drawdown).
  - Expense shortfall now contributes to `summary.totalShortfall`.
  - Row-level `incomeTotal` and `expenseTotal` are now populated.
- Client store + request wiring:
  - `getCurrentConfig()` now emits full Phase 7 drawdown/event payloads (no rebalancing coercion).
  - Added rebalancing actions: target allocation update, glide-path enable, waypoint add/remove/update.
  - Added income/expense preset creation paths.
  - Added retirement-duration glide-path boundary clamping for first/last waypoint years.
- Client UI:
  - Replaced Phase-7 placeholder with `RebalancingConfig` panel:
    - Target allocation % inputs with total validation display.
    - Glide-path toggle + waypoint editor + lightweight preview.
  - Upgraded income/expense cards with:
    - start + end date controls
    - frequency controls
    - inflation toggle
    - add-preset quick buttons
  - `Run Simulation` is blocked in UI when Rebalancing target allocation total != 100%.
- Tests:
  - Added `packages/server/tests/engine/drawdown/rebalancing.test.ts`.
  - Added `packages/server/tests/engine/events.test.ts`.
  - Added simulator integration coverage for rebalancing + income + expenses in `simulator.test.ts`.

Tooling adjustment:
- Updated root scripts so `npm run typecheck` and `npm test` build shared first, preventing stale shared runtime/type artifacts during cross-package checks.

Verification run (all passing):
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm test`

2026-02-15 — Phase 8 (Monte Carlo) complete

Completed:
- Shared contracts/types:
  - Added `HistoricalEra` enum and `constants/eras.ts` definitions.
  - Added Monte Carlo domain types (`MonteCarloResult`, percentile curves, historical summaries) and extended `SimulateResponse` with mode/seed/mc payload.
  - Updated request validation so `selectedHistoricalEra` must be a known era key.
- Server engine/routes:
  - Added `engine/historicalData.ts` for CSV loading, era filtering, and per-asset summary stats.
  - Added `engine/monteCarlo.ts` for N-run historical sampling with replacement, seeded determinism, per-asset percentile aggregation, terminal values, PoS, and representative median-like path.
  - Extended `POST /api/v1/simulate` to execute manual or Monte Carlo based on `config.simulationMode`.
  - Added `GET /api/v1/historical/summary` and registered route in `app.ts`.
- Client UI/state:
  - Added Monte Carlo era selector in `CommandBar` (#11a) and wired summary fetch on era changes.
  - Replaced Return Assumptions content with `HistoricalDataSummary` in Monte Carlo mode.
  - Added confidence bands + median line rendering in `PortfolioChart` with MC percentile tooltip values.
  - Added Probability of Success stat card (#41) with color thresholds (green >=90%, amber 75-89%, red <75%).
- Tests:
  - Added `historicalData.test.ts` (CSV parsing + era bounds + summary sanity).
  - Added `monteCarlo.test.ts` (determinism, 100%/0% PoS extremes, per-asset percentile aggregation, 1000-run performance).
  - Added `routes/historical.test.ts` and extended `routes/simulation.test.ts` with Monte Carlo response/plausibility coverage.

Verification run (all passing):
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm test`

## 2026-02-15 20:30 — SESSION START | Resuming from P12Z-T1. Last completed: Phase 12 closeout + doc reconciliation. Next: new Phase 13 theming foundation.

## 2026-02-15 20:30 — P13-T6 | COMPLETE

Implemented Phase 13 server-defined theming end-to-end: shared theme contracts/schemas, server theme registry + `/api/v1/themes`, client theme state + precedence resolution (`Snapshot > local preference > server default`), CSS-token theme engine, command-bar theme selector, and tokenized chart/stress/detail-table state colors.

[DECISION] Theme definitions are server-authored and client-selected by `ThemeId` only; no client-authored custom themes in Phase 13.
[DECISION] Built-in themes shipped: Light, Dark, High Contrast.
[ASSUMPTION] Color accessibility checks emit warnings (`ThemeValidationIssue`) rather than hard-failing startup to preserve intended visual identity while exposing contrast risk.

Validation passed: `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build`.
## 2026-02-15 19:47 — P12Z-T1 | COMPLETE

Completed Phase 12 pragmatic closeout verification and documentation update.
[DECISION] CSV export (#56) is intentionally out of scope for this release and documented as an accepted scope delta in `docs/ENGINEERING.md`.
[DEFERRED] P12D-T4 (manual-mode Detail Ledger header whitespace) remains explicitly deferred and tracked in `TASKS.md`.

Full regression gate passed: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build`.

## 2026-02-15 19:40 — P12F-T1 | COMPLETE

Implemented Phase 12F loading-state UX for running simulations:
- Summary stats now shows a running-state loading indicator.
- Portfolio chart and Detail Ledger table now show centered loading overlays while simulation status is `running`.
- Existing cached content remains visible behind overlays for continuity during recomputation.

Validation passed: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 19:33 — P12E-T1 | COMPLETE

Phase 12E Stress Test update: changed default panel state to expanded (`stress.isExpanded = true`) so Stress Test content is visible by default.
Collapse/expand toggle behavior is unchanged.

Validation passed: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 19:27 — P12D-T4 | DEFERRED

User-approved deferral of remaining manual-mode `Detail Ledger` header whitespace issue after multiple sticky-offset fixes (including dynamic measured offset and scroll-state sync) did not fully resolve observed behavior.
[DEFERRED] Continue with next Phase 12 work and revisit this table-header spacing bug in a later focused pass.

## 2026-02-15 18:54 — P12D-T3 | COMPLETE

Implemented Phase 12D detail-table polish:
- Header controls updated: `Detail Ledger` title, right-aligned Monthly/Annual toggle, `Breakdown` label-style control, and icon-only expand/compress table mode button.
- Added keyboard-oriented cell interaction: click-to-select highlight, Tab/Shift+Tab traversal across cells, and Enter-to-edit for editable tracking cells with auto-selected value text.
- Monthly period labels now render as calendar dates (`YYYY-Mon`) anchored to `coreParams.retirementStartDate`; annual period labels now show calendar year.
- Added explicit Monte Carlo note that table values represent the median path.

Validation passed: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 18:47 — P12C-T3 | COMPLETE

Applied final chart/input QA polish requested after Phase 12B/12C review:
- Core Parameters copy/format updates (`Withdrawals Start`, inline `yrs`, revised labels).
- Historical Data header rename (`Returns`, `Volatility`) and spacing/alignment fixes.
- Breakdown control converted from switch to label-style toggle, then tuned to smaller non-bold text with brighter active blue.

Validation passed during iteration: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 18:37 — P12C-T3 | COMPLETE

Implemented Phase 12C chart-panel polish:
- Replaced `By Asset Class` wording with `Breakdown` and moved it to the right of the Nominal/Real toggle.
- Switched default chart display mode to Real.
- Removed the zoom slider container so chart shows full-horizon by default.
- Added Monte Carlo y-axis focus scaling using median/IQR-oriented ceiling so P50 trajectory is more legible while upper percentile bands may clip above the chart.

Validation passed: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 17:02 — P12B-T3 | COMPLETE

Implemented Phase 12B input-panel polish:
- Core Parameters now uses two rows (`Starting Age`, `Withdrawals Start At`, `Expected Inflation Rate`) and (`Retirement Start Date`, `Retirement Duration`).
- Withdrawal Strategy guidance now shows concise summary by default with an expand/collapse details panel covering parameter effects and strategy tradeoff commentary.
- Historical Data summary table was compressed to four fixed columns without horizontal scroll and now includes era-specific commentary text below the table.

Validation passed: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 16:57 — P12A-T2 | COMPLETE

Addressed navbar QA feedback: removed the visible gap in the Manual/Monte Carlo segmented toggle and added explicit hover tooltips to Save/Load snapshot icon buttons.
Updated `SegmentedToggle` layout to equal-width grid pills and added tooltip UI wrappers in `CommandBar`.

Validation passed: `npm run typecheck -w @finapp/client` and `npm run lint -w @finapp/client`.

## 2026-02-15 16:44 — SESSION START | Resuming from Phase 12, Task P12A-T1. Last completed: P11-T6 (packed snapshot output format).

Read startup context from `AGENTS.md`, `PROGRESS.txt`, and `TASKS.md`, then reviewed Phase 12 requirements in `docs/ENGINEERING.md` before implementation planning.

## 2026-02-15 16:44 — P12A-T2 | COMPLETE

Implemented Phase 12A navbar UX refresh in `packages/client/src/components/layout/CommandBar.tsx`: added fixed left FinApp brand block, compact command-group labels, right-aligned Run Simulation action cluster, and icon-only Save/Load snapshot controls with tooltip titles and accessibility labels.
Tracking controls were kept near mode controls, Monte Carlo historical era remains conditional, and existing command behaviors were preserved.

Verification passed: `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build`.

2026-02-15 — Phase 9 (Tracking Mode) implementation pass

Completed:
- Shared contracts/domain:
  - Added `ActualMonthOverride` + `ActualOverridesByMonth` in shared domain.
  - Extended `SimulateRequest` with optional `actualOverridesByMonth`.
  - Added `ReforecastRequest` / `ReforecastResponse` contracts.
  - Added `reforecastRequestSchema` and actual-override schema validation.
- Server:
  - Added deterministic engine: `packages/server/src/engine/deterministic.ts`.
  - Added `POST /api/v1/reforecast` route: `packages/server/src/routes/reforecast.ts`.
  - Registered reforecast route in server app bootstrap.
- Client state and APIs:
  - Added `runReforecast` API wrapper.
  - Added Tracking actual override state/actions:
    - `actualOverridesByMonth`, `lastEditedMonthIndex`
    - `upsertActualOverride`, `clearActualRowOverrides`, `clearAllActualOverrides`
  - Added mode-isolated workspace switching behavior via clone/snapshot swap on mode toggle:
    - first switch to Tracking clones Planning once and clears Tracking caches.
    - subsequent mode switches restore per-mode inputs/results.
  - Active result selector now prefers `reforecast` in Tracking + Manual mode.
- Detail table Tracking UX:
  - Monthly editable cells for start balances, by-asset withdrawals, income, expenses.
  - Edited value visuals: tint + bold + dot marker.
  - Row-region tint through latest edited month.
  - Row reset action in monthly view.
  - Stale pill in Tracking + Monte Carlo when MC is stale.
  - Tracking-first empty-state prompt now requires initial run to bootstrap editable rows.
- Chart Tracking UX:
  - Added boundary marker and label: `Actuals -> Simulated`.
  - Added stale dim + warning banner for Tracking + Monte Carlo stale state.
  - Added split rendering path for Tracking + Monte Carlo (realized left / simulated bands right).
- Command bar Tracking UX:
  - Added Actuals watermark text (`No actuals entered` / `Actuals through: MMM YYYY`).
  - Added `Clear Actuals` action in Tracking mode.
- Tests:
  - Added `packages/server/tests/engine/deterministic.test.ts`.
  - Added `packages/server/tests/routes/reforecast.test.ts`.

Verification run (all passing):
- `npm run build`
- `npm run typecheck`
- `npm run lint`
- `npm test`
- Follow-up UX fix: Tracking table now renders edited override values directly in cells (not just markers), including frozen MC stale state.
- Phase 9 post-review defect fixes:
  - Fixed Tracking + Manual first-run behavior: deterministic reforecast is now gated behind actual edits (`lastEditedMonthIndex !== null`), so initial Tracking manual runs remain stochastic.
  - Fixed active-result selection in Tracking + Manual: reforecast is only preferred when actual edits exist; otherwise manual simulation result is shown.
  - Fixed Tracking + Monte Carlo override propagation: `/api/v1/simulate` now passes `actualOverridesByMonth` into both manual and Monte Carlo engines.
  - Extended `simulateRetirement` to honor month-level overrides for start balances, by-asset withdrawals, income, and expenses.
  - Extended Monte Carlo runner to apply the same actual overrides on each path before percentile aggregation.
- Added regression tests:
  - `packages/server/tests/engine/simulator.test.ts`: verifies overrides affect start balances, withdrawals, and end balances.
  - `packages/server/tests/routes/simulation.test.ts`: verifies overrides are applied for both Manual and Monte Carlo `/simulate` responses.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Phase 9 tracking behavior fix (manual edit recalculation semantics):
  - Replaced Tracking+Manual edit recalculation path in `DetailTable` from `/reforecast` deterministic run to `/simulate` with explicit `monthlyReturns` derived from the current manual result rows.
  - Effect: editing in Tracking+Manual now preserves existing stochastic month returns and recomputes congruently from edited context, instead of visually turning the whole path deterministic.
  - Because row-level values are now recomputed through the main simulator, same-month derived fields stay congruent after start-balance edits (e.g., `Start Total`, `Market Move`, `Return %`, per-asset move columns).
- Files updated:
  - `packages/client/src/components/output/DetailTable.tsx`

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Phase 9 tracking follow-up fix (manual boundary preservation + row congruence display):
  - Updated `packages/client/src/components/output/DetailTable.tsx` recalculation merge behavior so Tracking+Manual keeps rows through `lastEditedMonthIndex` from the visible result and only takes recomputed rows after that boundary.
  - Added start-balance delta propagation in detail-table display for edited rows:
    - `Start Total`, `Market Move`, `Return %`
    - `Move Stocks`, `Move Bonds`, `Move Cash`
  - Updated edit-entry prefill to use displayed (override-aware) values.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Phase 9 tracking defect follow-up:
  - Fixed Tracking+Manual `Run Simulation` behavior to preserve rows through `lastEditedMonthIndex` (inclusive) and recompute only future rows in the displayed result.
  - Fixed same-row congruence for start-balance edits to include end columns:
    - `End Stocks`, `End Bonds`, `End Cash`, and `End Total` now derive from override-adjusted start/move/withdrawal values.
- Files updated:
  - `packages/client/src/components/layout/CommandBar.tsx`
  - `packages/client/src/components/output/DetailTable.tsx`

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Tracking+Manual run fix after table edit:
  - Updated `packages/client/src/components/layout/CommandBar.tsx` so `Run Simulation` in Tracking+Manual with edits no longer reuses the exact prior monthly returns path.
  - Run now performs a fresh stochastic simulation with overrides, then preserves rows through the edit boundary in the displayed result.
  - This restores visible run behavior after edits while keeping historical/edited rows fixed.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- Tracking+MonteCarlo preservation fix:
  - Updated `packages/client/src/components/layout/CommandBar.tsx` to apply edit-boundary row preservation for Tracking+MonteCarlo runs, matching Tracking+Manual behavior.
  - On Run in Tracking mode with `lastEditedMonthIndex` set (Manual or MonteCarlo), simulation result rows now preserve months through the edited boundary and only replace future months.
- UX polish updates:
  - Summary stats terminal card now consistently shows `Portfolio End (Real)` in both Manual and Monte Carlo modes.
  - Portfolio chart SVG now uses `preserveAspectRatio="none"` so the plotted area fills the container width on large screens.
  - Detail-table preserved-row tint increased from very light blue to a darker shade for clearer distinction.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Phase 10 implemented: Stress Testing vertical slice.
  - Added shared stress domain/contracts/schemas:
    - `StressScenario` discriminated union (stock/bond/broad crash, prolonged bear, inflation spike, custom)
    - `StressTestRequest` / `StressTestResponse`
    - `StressTestResult` + scenario comparison/timing shapes.
  - Server:
    - Added `engine/stress.ts` for scenario overlays, base comparison metrics, and manual timing sensitivity sweeps.
    - Extended `simulateRetirement` with optional inflation-by-year overrides for inflation stress scenarios.
    - Extended `runMonteCarlo` with optional return-transform and inflation-override hooks for stress overlays.
    - Added `POST /api/v1/stress-test` route and registered route plugin.
  - Client:
    - Added stress API wrapper `packages/client/src/api/stressApi.ts`.
    - Added workspace-isolated stress state/actions in `useAppStore`.
    - Added `StressTestPanel` with collapsible UI, scenario cards, add/remove (max 4), auto recompute, comparison bars/table, and manual-only timing chart.
    - Integrated panel in output flow below detail table.
  - Tests added:
    - `packages/server/tests/engine/stress.test.ts`
    - `packages/server/tests/routes/stress.test.ts`

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Phase 10 UX and correctness refinements:
  - Main portfolio chart now overlays stress scenario paths with per-scenario legend.
  - Fixed Manual stress comparison path consistency:
    - Added `monthlyReturns` to stress request contract/schema.
    - Client now sends inferred base monthly returns for manual stress runs.
    - Server stress engine now uses provided monthly returns so base vs scenario shares identical underlying path.
  - Main chart tooltip now includes stress scenario point-in-time values (portfolio + withdrawal).
  - Tooltip clipping fix:
    - Chart keeps SVG clipped while tooltip renders in overflow-visible layer above container.
  - Stress panel display refresh:
    - Replaced large horizontal bars with compact vertical charts.
    - Added Median and Mean Monthly Withdrawal comparison charts.
    - Removed Timing Sensitivity chart from Stress panel UI.
    - Compacted metrics table spacing and typography.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Documentation alignment pass completed to reflect implemented behavior as source of truth:
  - Updated `docs/PRD.md` for stateless backend wording, independent Planning/Tracking workspaces, and stress overlay framing.
  - Updated `docs/SPECS.md` for workspace isolation, Tracking initialization/edit boundary semantics, stale indicators, `Portfolio End (Real)` stat label, annualized historical summary labels, spreadsheet mode, expanded editable columns, and stress panel chart changes (compact vertical charts, no timing-sensitivity panel).
  - Updated `docs/SCENARIOS.md` narratives for annualized historical summary, Tracking initialization run, MC stale UX, stress chart overlays, and portfolio-end terminology.
  - Replaced `docs/API.md` with current implemented `/api/v1` contract surface (`/health`, `/simulate`, `/historical/summary`, `/reforecast`, `/stress-test`).
  - Updated `docs/ARCHITECTURE.md` and `docs/ENGINEERING.md` with implementation deltas for workspace-isolated state and final stress UI direction.
- [ASSUMPTION] Phase 11 scope intentionally de-scoped explicit app-level Undo/Redo (#62/#63) in favor of snapshot-only recovery; browser-native field undo remains incidental and non-contractual.
- Phase 11 implementation completed (snapshot persistence):
  - Added client snapshot module `packages/client/src/store/snapshot.ts` with JSON envelope serialization, strict schema version matching, Zod validation, sanitized filenames, and full-state restore.
  - Added Save/Load Snapshot controls to `packages/client/src/components/layout/CommandBar.tsx` (prompt for name, file picker load, replacement confirmation, and user-visible success/error messaging).
  - Extended store typing/actions in `packages/client/src/store/useAppStore.ts` to expose `SnapshotState`, produce snapshot state, and apply restored snapshot state atomically.
  - Added client tests `packages/client/src/store/snapshot.test.ts` covering round-trip restore (including cached outputs), strict version mismatch rejection, and invalid snapshot no-mutation behavior.
  - Updated documentation and planning artifacts for snapshot-only Phase 11: `docs/ENGINEERING.md`, `docs/SPECS.md`, `docs/PRD.md`, `docs/SCENARIOS.md`, `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, and `TASKS.md`.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Phase 11 snapshot size optimization completed:
  - Snapshot format upgraded to schemaVersion `3` with strict exact-version loading.
  - Implemented packed cached-output row encoding in `packages/client/src/store/snapshot.ts`:
    - New `rowsPacked` structure uses fixed `columns` metadata and numeric `data` tuples (CSV-like arrays).
    - Applied to all cached outputs in snapshots: `manual`, `monteCarlo`, `reforecast`, and stress base/scenario result rows (including workspace snapshots).
    - Decode-on-load restores canonical in-memory row objects, so chart/stats/table consumers remain unchanged.
  - Snapshot JSON now serializes in compact form to further reduce payload size.
  - Expanded snapshot tests in `packages/client/src/store/snapshot.test.ts` for malformed packed column headers and malformed row widths.
  - Updated `docs/SPECS.md`, `docs/ENGINEERING.md`, `docs/ARCHITECTURE.md`, `docs/DATA_MODEL.md`, `docs/SCENARIOS.md`, and `TASKS.md` to reflect packed snapshot output storage.
- Phase 12 documentation reconciliation pass completed (post-polish deltas):
  - Updated `docs/SPECS.md` to reflect shipped chart/detail/stress behavior: chart defaults to Real, label-style Breakdown controls, zoom/pan deferred, Detail Ledger naming/controls, CSV export deferred, and Stress panel default expanded.
  - Updated `docs/SCENARIOS.md` to align narratives with current UX (Detail Ledger naming, Breakdown control labels, no active zoom/pan flow, loading-state scenario, and no CSV-export scenario).
  - Updated `docs/ARCHITECTURE.md` to remove active CSV-export/zoom assumptions from current UI architecture, align component naming, and annotate deferred/reserved areas.
  - Updated `docs/PRD.md`, `docs/DATA_MODEL.md`, and `docs/API.md` with current-release scope and defaults (CSV deferred, `chartDisplayMode` default real, `stress.isExpanded` default true, `chartZoom` reserved).
- Phase 13 pre-implementation documentation update completed for Adaptive Dynamic SWR variant:
  - Updated `docs/WITHDRAWAL_STRATEGIES.md` with new **Strategy 5B: Adaptive Dynamic SWR (Monthly Realized TWR)**, including formulas for trailing real TWR (per-asset), weight aggregation, fallback ROI warm-up, and monthly-cadence recalculation details.
  - Updated `docs/SPECS.md` withdrawal strategy UX/spec to include **Dynamic SWR (Adaptive TWR)** in selector/tooltip/parameter panel, plus monthly-cadence exception to annual strategy behavior.
  - Updated `docs/DATA_MODEL.md` and `docs/API.md` to add `withdrawalStrategy.type = "dynamicSwrAdaptive"` with params `{ fallbackExpectedRateOfReturn, lookbackMonths }`.
  - Updated `docs/ARCHITECTURE.md` simulation/strategy architecture notes to document annual vs monthly strategy cadence and context requirements for realized-return-driven calculation.
  - Updated `docs/PRD.md` and `docs/SCENARIOS.md` strategy counts from 12 to 13.
- Implemented Adaptive Dynamic SWR feature (new withdrawal strategy variant):
  - Added `WithdrawalStrategyType.DynamicSwrAdaptive` and shared model/schema support with params `{ fallbackExpectedRateOfReturn, lookbackMonths }`.
  - Added server strategy implementation in `packages/server/src/engine/strategies/dynamicSwrAdaptive.ts` using trailing realized real TWR (per-asset), start-of-month weighted aggregation, nominal ROI conversion, fallback ROI warm-up, and monthly withdrawal output.
  - Updated simulation engines (`simulator.ts`, `deterministic.ts`) to support strategy-level monthly cadence for Adaptive Dynamic SWR while preserving annual cadence for all other strategies.
  - Wired client selector/params/tooltip support for "Dynamic SWR (Adaptive TWR)" in Withdrawal Strategy UI and store config mapping.
  - Added/updated tests: new adaptive strategy unit tests and simulator coverage for monthly recalculation behavior; expanded strategy sweep from 12 to 13 strategies.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Fixed Adaptive Dynamic SWR spending-phase clamp behavior:
  - Updated `packages/server/src/engine/simulator.ts` to apply month-level inflation-adjusted min/max monthly spending bounds for monthly-cadence strategies (`dynamicSwrAdaptive`) instead of year-stepped annual/12 bounds.
  - Updated `packages/server/src/engine/deterministic.ts` with the same month-level clamp behavior for deterministic reforecast.
  - Added regression coverage in `packages/server/tests/engine/simulator.test.ts` to verify adaptive strategy monthly max clamp rises month-over-month under positive inflation.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- Adaptive SWR performance optimization implemented (no API/UX changes):
  - Added rolling trailing-return helper `packages/server/src/engine/helpers/rollingWindow.ts` to compute annualized realized returns in O(1) per month instead of re-slicing/reducing trailing arrays.
  - Added monthly inflation factor precompute helper `buildMonthlyInflationFactors` in `packages/server/src/engine/helpers/inflation.ts`.
  - Updated `packages/server/src/engine/simulator.ts` and `packages/server/src/engine/deterministic.ts`:
    - Adaptive strategy now consumes precomputed annualized realized returns from rolling tracker.
    - Adaptive monthly min/max clamping now uses O(1) lookup from precomputed monthly inflation factors.
    - Non-adaptive strategies remain unchanged in behavior.
  - Extended strategy context with optional `annualizedRealReturnsByAsset` to support optimized path while preserving fallback compatibility.
  - Added tests:
    - `packages/server/tests/engine/helpers/rollingWindow.test.ts`
    - `packages/server/tests/engine/helpers/inflation.test.ts`
    - Extended `packages/server/tests/engine/strategies/dynamicSwrAdaptive.test.ts` for precomputed annualized-return path.

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`

## 2026-02-16 03:41 — FEATURE-SYNTHWAVE84-BUILD | COMPLETE

Implemented Synthwave '84 as a new built-in server-defined theme: added `ThemeId.Synthwave84`, added a full Synthwave token bundle in `packages/server/src/themes/registry.ts`, and extended `/api/v1/themes` route tests to include Synthwave in built-in IDs.

Validation passed: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build`.
[QUESTION] Manual visual/persistence acceptance in `docs/features/themes/synthwave84/ACCEPTANCE.md` is still pending user verification.
## 2026-02-16 03:39 — FEATURE-SYNTHWAVE84-PLAN | COMPLETE

Created a decision-complete planning pack for Synthwave '84 under `docs/features/themes/synthwave84/` with implementation plan, task breakdown, acceptance criteria, and token-direction guidance.

[DECISION] Canonical docs were not updated at planning time because this feature is additive within existing server-defined themes architecture.
## 2026-02-16 02:34 — PHASE13-BUGFIXES | COMPLETE

Fixed two post-Phase-13 UX defects in client UI state/layout:
1) `CollapsibleSection` no longer clips long Additional Income / Large Expenses lists (replaced fixed max-height with measured animated height via `scrollHeight` + `ResizeObserver`).
2) Snapshot Load file picker no longer restricts file names via `accept` filter; loader now relies strictly on JSON/schema validation.

Validation passed: `npm run test -w @finapp/client`, `npm run typecheck -w @finapp/client`, and `npm run lint -w @finapp/client`.
## 2026-02-16 01:56 — FEATURE-MONOKAI-FOLLOWUP | COMPLETE

Fixed theme dropdown getting stuck on "Loading..." after snapshot load. Root cause: theme hydration fetch ran only on initial mount, so snapshot-restored empty/loading theme state had no refetch path.

Updated `packages/client/src/App.tsx` to rehydrate themes whenever catalog/definitions are missing, with an in-flight guard to prevent duplicate fetches. Client `test`, `typecheck`, and `lint` all pass.
## 2026-02-16 01:38 — FEATURE-MONOKAI-BUILD | COMPLETE

Implemented Monokai as a new built-in server-defined theme: added `ThemeId.Monokai`, added a full Monokai token bundle in `packages/server/src/themes/registry.ts`, and updated `/api/v1/themes` route tests to assert Monokai is included in built-in IDs.

Validation passed: `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build`.
[QUESTION] Manual visual acceptance (`docs/features/themes/monokai/ACCEPTANCE.md`) is still pending user review, including live picker rendering and persistence smoke checks.
## 2026-02-16 01:34 — FEATURE-MONOKAI-PLAN | COMPLETE

Created a decision-complete feature planning pack under `docs/features/themes/monokai/` with implementation plan, task breakdown, acceptance checklist, and token-direction guidance.

[DECISION] No canonical doc updates were made in planning because Monokai is additive within the existing Phase 13 server-defined theming architecture.

2026-02-17 (Phase 14 Compare Portfolios — core implementation)
- Implemented Compare mode foundations across shared and client store:
  - Added `AppMode.Compare`.
  - Added compare workspace state (`leftWorkspace`, `rightWorkspace`, `activeSlot`) with mode-switch seeding rules.
  - First switch into Compare now seeds Slot A from currently open workspace and initializes Slot B as clone of Slot A.
  - Added `setCompareActiveSlot`, per-slot compare run status/result actions, and compare selectors.
- Implemented Compare run orchestration in command bar:
  - Run Simulation in Compare mode issues dual `/simulate` calls.
  - Shared seed parity is applied across A/B runs to keep stochastic randomness aligned.
  - Per-slot errors/status are tracked.
- Implemented Compare UI core:
  - Command bar mode selector now includes Compare.
  - Sidebar now includes `Portfolio A / Portfolio B` slot switcher.
  - Summary stats now render A/B comparisons with delta values in Compare mode.
  - Portfolio chart now renders dual A/B paths in Compare mode.
  - Detail table now renders side-by-side ledgers in Compare mode with shared granularity/breakdown controls.
- Implemented snapshot compatibility for Compare:
  - Snapshot schema now supports optional compare workspace payload for backward compatibility.
  - Compare-mode load flow supports target prompts (`left`/`right`/`both`) and pair-slot selection (`A`/`B`) for single-side imports.
- Validation gates (all passing):
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `npm run build`
- Remaining for Phase 14 completion:
  - Stress Test panel is not yet fully dual-slot compare-aware (currently core compare outputs are implemented for chart/stats/detail ledger).
- Phase 14 bug-fix pass: compare chart and output UX corrections.
  - Fixed Compare chart rendering to include robust axes/grid/ticks and hover tooltip for both slots.
  - Resolved missing Portfolio A line by correcting chart stroke token to `--theme-chart-manual-line` and drawing A over B.
  - Updated Compare stats cards so Portfolio A and B use same-size typography and chart-matching colors; delta is shown as annotation.
  - Added synchronized vertical + horizontal scroll locking between Compare Detail Ledger panes.
- Validation (client):
  - `npm run typecheck -w @finapp/client`
  - `npm run lint -w @finapp/client`
  - `npm run build -w @finapp/client`
- Phase 14 bug-fix pass: Compare + Monte Carlo parity and rendering.
  - Fixed compare slot result mode persistence by storing slot `simulationMode` when compare results are saved.
  - Fixed compare output selectors (stats/table/chart) to resolve per-slot result by currently selected simulation mode with safe fallback.
  - Implemented Compare-mode Monte Carlo chart overlays for both portfolios: P10-P90 and P25-P75 bands plus P50 median line per slot.
  - Updated compare MC legend labels to indicate percentile band rendering.
  - Verified compare stochastic fairness: both slots continue using shared seed per run invocation.
- Validation (client):
  - `npm run typecheck -w @finapp/client`
  - `npm run lint -w @finapp/client`
  - `npm run build -w @finapp/client`
- Phase 14/MC refinement: representative-path tie-break update.
  - Updated `runMonteCarlo` representative-path selection to use deterministic tie-break ordering:
    1) min terminal-distance to median terminal,
    2) min drawdown-distance to median total withdrawn,
    3) min total shortfall,
    4) stable first-match index.
  - Added MC engine test coverage asserting terminal-distance minimization and deterministic tie-break stability under fixed seed.
- Validation:
  - `npm test -w @finapp/server -- monteCarlo.test.ts`
  - `npm run typecheck`
  - `npm run lint`
- Phase 14 compare/stress chart UX fixes.
  - Added stress scenario overlays to Compare-mode chart rendering (previously missing in compare branch).
  - Standardized line semantics: Portfolio A/B lines are solid; stress scenario overlays are dotted.
  - Decoupled compare portfolio palette from stress palette: Portfolio B now uses dedicated compare color (`--theme-color-info`) across chart lines, MC bands, legend, tooltip markers, and compare stat cards.
- Validation (client):
  - `npm run typecheck -w @finapp/client`
  - `npm run lint -w @finapp/client`
  - `npm run build -w @finapp/client`
- Task tracking reconciliation pass (Phase 14 + feature docs).
  - Reconciled root `TASKS.md` Phase 14 status: marked `P14-T5` complete to match implemented compare stress dual-slot output and manual QA coverage.
  - Removed feature-local task checklists to eliminate split tracking sources:
    - `docs/features/compare-portfolios/TASKS.md`
    - `docs/features/themes/monokai/TASKS.md`
    - `docs/features/themes/synthwave84/TASKS.md`
  - Updated feature plan/acceptance docs that referenced local `TASKS.md` files to point to root `TASKS.md` as canonical.
- Process docs update: post-V1 defect workflow standardized.
  - Added docs-first defect backlog index at `docs/issues/INDEX.md`.
  - Added per-bug template set under `docs/issues/_TEMPLATE/`:
    - `ISSUE.md`
    - `PLAN.md`
    - `ACCEPTANCE.md`
    - `NOTES.md` (optional investigation log template)
  - Updated `AGENTS.md` with Section 8 defect workflow rules:
    - Folder-per-bug policy at `docs/issues/BUG-####-<slug>/`
    - Required files and optional `NOTES.md`
    - ID assignment convention (`BUG-####`) and tracking linkage
    - Mandatory `Canonical Docs Impact` section in bug plans
  - Updated `README.md` and `docs/ENGINEERING.md` to cross-reference defect workflow.
  - Updated root `TASKS.md` with completed process task `PROC-ISSUES-T1`.
- Process docs update: existing-feature update/versioning convention.
  - Added explicit rules in `AGENTS.md` for post-V1 updates to existing features:
    - original feature folder remains immutable baseline,
    - new versioned folder per approved batch (`<feature>-v1-1`, `<feature>-v1-2`, ...),
    - major bump (`v2-0+`) only for contract/model breaks,
    - required `Delta From Baseline` section in update `PLAN.md`.
  - Added `docs/features/CONVENTIONS.md` as the canonical quick reference for feature-folder structure and versioning.
  - Updated `README.md` and `docs/ENGINEERING.md` with cross-references to the new convention.
  - Updated root `TASKS.md` with completed process task `PROC-FEATURE-UPDATES-T1`.
- [DECISION] Introduced a third post-V1 documentation lane for minor refinements under `docs/changes` to avoid overloading `docs/features` and `docs/issues`.
- CHG-0001 workflow implementation (process/docs only):
  - Added `docs/changes/INDEX.md` with impact-based classification rules and `CHG-####` ID policy.
  - Added lightweight templates:
    - `docs/changes/_TEMPLATE/CHANGE.md`
    - `docs/changes/_TEMPLATE/ACCEPTANCE.md`
  - Added `docs/changes/CONVENTIONS.md` to define required files, routing, and root-tracker linkage.
  - Added sample entry:
    - `docs/changes/CHG-0001-minor-change-workflow-smoke/CHANGE.md`
    - `docs/changes/CHG-0001-minor-change-workflow-smoke/ACCEPTANCE.md`
  - Updated canonical/process docs to include the new lane and decision tree:
    - `AGENTS.md`
    - `README.md`
    - `docs/ENGINEERING.md`
    - `docs/features/CONVENTIONS.md`
  - Updated root tracker with completed process task `PROC-CHANGES-T1` in `TASKS.md`.
- BUG-0001 compare stress dual-plot fix implemented.
  - Added defect artifacts:
    - `docs/issues/BUG-0001-compare-stress-dual-plot/ISSUE.md`
    - `docs/issues/BUG-0001-compare-stress-dual-plot/PLAN.md`
    - `docs/issues/BUG-0001-compare-stress-dual-plot/ACCEPTANCE.md`
  - Updated `docs/issues/INDEX.md` with BUG-0001 (status: Ready for QA).
  - Updated root `TASKS.md` with completed task `BUG-0001-T1`.
  - Store changes (`packages/client/src/store/useAppStore.ts`):
    - Added compare-slot stress actions: `setCompareSlotStressStatus`, `setCompareSlotStressResult`, `clearCompareSlotStressResult`.
    - Added selector `useCompareStressResults`.
  - Stress panel changes (`packages/client/src/components/output/StressTestPanel.tsx`):
    - Compare mode now runs stress test per slot (`left`, `right`) using slot-specific config/base/actual overrides.
    - Persists stress result/status per compare slot.
    - Preserves successful slot output when the other slot fails and surfaces slot-specific error text.
  - Chart changes (`packages/client/src/components/output/PortfolioChart.tsx`):
    - Compare mode stress overlays now render two lines per scenario when available: `<Scenario> (A)` and `<Scenario> (B)`.
    - Same scenario hue is shared; `(A)` is solid and `(B)` is dashed.
    - Compare legend now includes scenario `(A)/(B)` entries.
    - Compare tooltip now includes stress scenario `(A)/(B)` portfolio + withdrawal values.
  - Canonical docs impact: none (defect correction only; tracked in bug PLAN).

Verification run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- BUG-0001 closure update.
  - Addressed follow-up compare-mode stress interaction defect causing scenario cards to disappear after `+ Add Scenario` due to active-slot stress state writes overwriting shared scenario definitions.
  - Updated compare-slot stress actions in `packages/client/src/store/useAppStore.ts` to preserve global `stress.scenarios` while synchronizing only status/error/result.
  - Marked BUG-0001 acceptance checklist complete and set issue status to `Done` in:
    - `docs/issues/BUG-0001-compare-stress-dual-plot/ACCEPTANCE.md`
    - `docs/issues/BUG-0001-compare-stress-dual-plot/ISSUE.md`
    - `docs/issues/INDEX.md`

Validation run (client):
- `npm run typecheck -w @finapp/client`
- `npm run build -w @finapp/client`
- CHG-0002 kickoff (docs-first).
  - Created change folder and draft docs:
    - `docs/changes/CHG-0002-mc-start-total-p50-column/CHANGE.md`
    - `docs/changes/CHG-0002-mc-start-total-p50-column/ACCEPTANCE.md`
  - Added backlog entry in `docs/changes/INDEX.md`.
  - Mirrored active task in root `TASKS.md` as `CHG-0002-T1`.
- CHG-0002 complete: Monte Carlo Detail Ledger `Start Total (p50)` reference column.
  - Added docs-first minor-change artifacts and backlog tracking:
    - `docs/changes/CHG-0002-mc-start-total-p50-column/{CHANGE.md,ACCEPTANCE.md}`
    - `docs/changes/INDEX.md`
    - Root `TASKS.md` (`CHG-0002-T1`)
  - Implemented client data + UI changes:
    - Extended `DetailRow` with `startTotalP50` in `packages/client/src/lib/detailTable.ts`.
    - Added MC mapping rules: month 1 baseline start total; month N uses prior-month p50 terminal value; annual uses first month of year.
    - Added `Start Total (p50)` column in MC mode only, positioned between `Age` and `Start Total`, with subtle reference-column tinting.
    - Added header info tooltip explainer text in `packages/client/src/components/output/DetailTable.tsx`.
    - Applied behavior in single and compare detail ledgers.
  - Added tests:
    - `packages/client/src/lib/detailTable.test.ts` covers monthly, annual, and null-series behavior for `startTotalP50`.
  - Canonical docs updated for UX behavior change:
    - `docs/SPECS.md`
    - `docs/SCENARIOS.md`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- CHG-0003 started: compare slot theme-adaptive visual refinement.
  - Created docs lane: `docs/changes/CHG-0003-compare-slot-theme-adaptive-visuals/{CHANGE,ACCEPTANCE}.md`.
  - Updated `docs/changes/INDEX.md` with CHG-0003 (`In Progress`).
  - Added root tracker entry `CHG-0003-T1` to `TASKS.md`.
  - Canonical docs update wave opened for token/API + compare visual spec alignment: `docs/API.md`, `docs/DATA_MODEL.md`, `docs/SPECS.md`, `docs/SCENARIOS.md`.

- CHG-0003 complete: compare slot theme-adaptive visual refinement implemented and validated.
  - Theme contract surface expanded for compare slots:
    - `packages/shared/src/domain/theme.ts` (`ThemeChartTokens.compareSlotA..compareSlotH`)
    - `packages/shared/src/contracts/schemas.ts` (`themeChartSchema` includes `compareSlotA..H`)
  - Theme plumbing and registry updates:
    - `packages/server/src/themes/registry.ts` populated compare slot chart tokens for Light, Dark, Monokai, Synthwave '84, and High Contrast.
    - `packages/client/src/styles/themeEngine.ts` now exports CSS vars `--theme-chart-compare-slot-a..h`.
    - `packages/client/src/styles/themeEngine.test.ts` updated for new token mapping assertions.
  - Compare slot color alignment implementation:
    - Added `packages/client/src/lib/compareSlotColors.ts` for canonical slot-ID to CSS var mapping.
    - `packages/client/src/components/output/PortfolioChart.tsx` now uses slot-ID token colors for all compare slot lines/legend entries.
    - `packages/client/src/components/layout/Sidebar.tsx` now uses slot-color backgrounds, slot-color baseline ring/badge, and solid red hover remove icon with high-contrast glyph.
    - `packages/client/src/components/output/DetailTable.tsx` compare tabs now use slot-color backgrounds and matching baseline ring/badge.
  - Canonical docs updated for this change:
    - `docs/API.md`, `docs/DATA_MODEL.md`, `docs/SPECS.md`, `docs/SCENARIOS.md`.
  - Change lane closed:
    - `docs/changes/CHG-0003-compare-slot-theme-adaptive-visuals/{CHANGE,ACCEPTANCE}.md` updated to Done.
    - `docs/changes/INDEX.md` status set to Done.
    - `TASKS.md` `CHG-0003-T1` marked complete.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

- CPV2-T8 complete: Compare v2.0 post-feedback UX refinement pass implemented.
  - Sidebar compare slot UX (`/Users/ricknayar/Development/codex/finapp/packages/client/src/components/layout/Sidebar.tsx`):
    - removed clone-source dropdown and remove button flow.
    - added slot-chip interactions: single-click active, double-click baseline, hover red `X` remove (hidden at 2 slots).
    - baseline chip now shows gold ring + `Base` badge.
    - `+` now clones the active slot only.
  - Compare ordering + fallback determinism (`/Users/ricknayar/Development/codex/finapp/packages/client/src/store/useAppStore.ts`):
    - enforced alphabetic `slotOrder` normalization after add/remove and snapshot normalization paths.
    - kept fill-first ID reuse behavior while rendering remains sorted.
    - baseline/active fallback on removal remains first sorted slot.
  - Targeted compare import ordering (`/Users/ricknayar/Development/codex/finapp/packages/client/src/components/layout/CommandBar.tsx`):
    - compare targeted slot import now explicitly re-sorts `slotOrder` after mutation.
  - Compare chart MC density policy (`/Users/ricknayar/Development/codex/finapp/packages/client/src/components/output/PortfolioChart.tsx`):
    - baseline-only percentile bands (p10/p90 + p25/p75) with p50 lines for all slots.
    - tooltip + legend now attribute percentile-band context to the baseline slot.
  - Compare detail ledger tab styling (`/Users/ricknayar/Development/codex/finapp/packages/client/src/components/output/DetailTable.tsx`):
    - compare tabs switched from pills to circular chips with baseline affordance parity.
  - Added compare store regression tests (`/Users/ricknayar/Development/codex/finapp/packages/client/src/store/useAppStore.compare.test.ts`):
    - clone-source behavior, alphabetical order normalization, active/baseline fallback, and ID reuse ordering.
  - Canonical docs impact: no additional root canonical doc deltas required beyond existing compare v2.0 updates; this wave is behavioral/UI refinement inside approved v2.0 scope.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Compare Portfolios v2.0 docs-first wave started (2..8 compare slots).
  - Added new feature update folder:
    - `docs/features/compare-portfolios-v2-0/FEATURE.md`
    - `docs/features/compare-portfolios-v2-0/PLAN.md`
    - `docs/features/compare-portfolios-v2-0/ACCEPTANCE.md`
  - Added root tracker section in `TASKS.md`:
    - `CPV2-T1` completed (docs baseline)
    - `CPV2-T2`..`CPV2-T7` queued for implementation
  - Updated canonical docs for compare v2.0 model:
    - `docs/SPECS.md`
      - Compare affordances updated from fixed A/B to variable slots (`A`..`H`, 2..8).
      - Run behavior updated to all-slot compare execution with shared stochastic parity.
      - Stats cards updated to multi-slot values + baseline-relative deltas.
      - Detail ledger updated to tabbed single-viewport compare model.
      - Snapshot affordances updated for multi-slot compare payload and deterministic legacy import mapping.
    - `docs/SCENARIOS.md`
      - Scenario #25 updated from A/B flow to multi-slot compare workflow.
    - `docs/DATA_MODEL.md`
      - Compare model updated from `left/right` pair to slot collection with `activeSlotId`, `baselineSlotId`, `slotOrder`, and `slots` map.
      - Slot bounds and baseline fallback behavior documented.
      - Snapshot compatibility model updated to support legacy single/pair + new multi-slot compare payload.
    - `docs/API.md`
      - Compare orchestration updated to per-slot calls (2..8) with bounded parallelism, no new endpoints.
    - `docs/ARCHITECTURE.md`
      - Compare state alignment and compare run pipeline updated for slot collection and bounded queue execution.
  - Canonical docs impact statement: compare v2.0 required updates to SPECS/SCENARIOS/DATA_MODEL/API/ARCHITECTURE; no PRD or ENGINEERING changes required for this batch.
- Compare Portfolios v2.0 implementation complete for root tasks `CPV2-T2`..`CPV2-T7`.
  - Store/model refactor (`packages/client/src/store/useAppStore.ts`):
    - Replaced fixed compare pair (`left/right`) with slot collection model (`A..H`) using:
      - `compareWorkspace.slotOrder` (2..8 active)
      - `compareWorkspace.slots` (slot-scoped workspaces)
      - `compareWorkspace.activeSlotId`
      - `compareWorkspace.baselineSlotId`
    - Added compare actions:
      - `setCompareBaselineSlot`
      - `addCompareSlotFromSource`
      - `removeCompareSlot`
      - generalized slot-scoped simulation/stress setters.
    - Added multi-slot config selector `getCompareConfigs()`.
  - Snapshot compatibility update (`packages/client/src/store/snapshot.ts`):
    - Snapshot packing now includes compare slot workspaces in packed-row form.
    - Added multi-slot compare unpack support.
    - Preserved backward compatibility for legacy pair shape (`activeSlot`, `leftWorkspace`, `rightWorkspace`) by mapping to slots `A/B`.
  - Command bar compare orchestration (`packages/client/src/components/layout/CommandBar.tsx`):
    - Compare run now executes active slots (2..8) via bounded parallel queue (`maxParallel = 4`) with shared seed parity.
    - Compare snapshot targeted import now supports slot target (`A..H`) or `ALL`, with deterministic source-slot mapping.
  - Compare slot manager UI (`packages/client/src/components/layout/Sidebar.tsx`):
    - Added slot chip selector, add/clone control, remove control, and baseline selector.
    - Applied visual direction inspired by provided mockup (chip/pill style + compact action row).
  - Output compare rendering updates:
    - `packages/client/src/components/output/SummaryStatsBar.tsx`
      - cards now render all active slot values and baseline-relative deltas.
    - `packages/client/src/components/output/PortfolioChart.tsx`
      - chart now renders all active slot forecast lines with shared tooltip and legend entries.
    - `packages/client/src/components/output/DetailTable.tsx`
      - replaced side-by-side pair panes with one shared ledger viewport + slot tabs.
    - `packages/client/src/components/output/StressTestPanel.tsx`
      - compare stress execution generalized to active slots with bounded parallel queue and slot-scoped error handling.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- CHG-0003 follow-up UX tweak (append-only log entry):
  - Updated baseline marker text in compare slot chips and compare ledger tabs to lightweight `Base` text below the circle with no overlap.
  - Files: `packages/client/src/components/layout/Sidebar.tsx`, `packages/client/src/components/output/DetailTable.tsx`.
  - Verified: `npm run typecheck -w @finapp/client`, `npm run lint -w @finapp/client`.
- CHG-0004 started: enforce compare slot `A` as non-deletable invariant.
  - Created docs lane: `docs/changes/CHG-0004-compare-slot-a-non-deletable/{CHANGE,ACCEPTANCE}.md`.
  - Updated `docs/changes/INDEX.md` with CHG-0004 (`In Progress`).
  - Appended `CHG-0004-T1` to `TASKS.md`.
  - Canonical docs impact expected: `docs/SPECS.md` (Affordance #67).
- CHG-0004 complete: compare slot `A` non-deletable invariant enforced.
  - Store guard (`packages/client/src/store/useAppStore.ts`): `removeCompareSlot('A')` now returns unchanged state.
  - Sidebar UX (`packages/client/src/components/layout/Sidebar.tsx`): remove affordance hidden for slot `A` (`canRemoveCompareSlot && slotId !== 'A'`).
  - Tests (`packages/client/src/store/useAppStore.compare.test.ts`): added coverage for `A` no-op removal, non-`A` removal with `A` present, and min-2 guard preservation.
  - Canonical doc update: `docs/SPECS.md` Affordance #67 now states slot `A` is required and non-removable.
  - Change lane closure: `docs/changes/CHG-0004-compare-slot-a-non-deletable/{CHANGE,ACCEPTANCE}.md` and `docs/changes/INDEX.md` marked Done.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- Compare Portfolios v2.1 docs-first wave started (differences-only parameter table).
  - Created `docs/features/compare-portfolios-v2-1/{FEATURE,PLAN,ACCEPTANCE}.md`.
  - Appended root tasks `CPV21-T1`..`CPV21-T4` in `TASKS.md`.
  - Canonical docs impact set: `docs/SPECS.md`, `docs/SCENARIOS.md`.
- Compare Portfolios v2.1 implementation complete for root tasks `CPV21-T1`..`CPV21-T4`.
  - New compare differences engine + tests:
    - `packages/client/src/lib/compareParameterDiffs.ts`
    - `packages/client/src/lib/compareParameterDiffs.test.ts`
    - Supports all editable input families and only emits cross-slot deltas.
    - Uses deterministic normalization/summaries for structured lists (spending phases, income events, expense events, glide path) and ignores ids where required.
  - New compare output panel:
    - `packages/client/src/components/output/CompareParameterDiffTable.tsx`
    - Rendered between SummaryStatsBar and PortfolioChart in compare mode via `packages/client/src/components/layout/AppShell.tsx`.
    - Run-gated behavior: panel shows only when active slots have current-mode complete run snapshots and at least one difference row.
    - Panel hides when no differences are present.
  - Simulation response snapshot expansion:
    - `packages/shared/src/contracts/api.ts` updates `SimulateResponse.configSnapshot` to full `SimulationConfig`.
    - `packages/server/src/routes/simulation.ts` now returns full run `configSnapshot: body.config` for manual and Monte Carlo.
    - Enables run-gated, non-live-edit compare parameter diffs across all editable families.
  - Canonical docs updated:
    - `docs/SPECS.md` (Affordance #73)
    - `docs/SCENARIOS.md` (compare workflow includes differences panel)
    - `docs/API.md` and `docs/DATA_MODEL.md` (SimulateResponse configSnapshot contract update)
    - feature wave docs in `docs/features/compare-portfolios-v2-1/`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- CPV21-T5 started: follow-up UX refinement for Compare differences panel.
  - Scope: replace raw strategy enum strings with plain-English labels and add collapsible panel container behavior.
- CPV21-T5 complete.
  - Diff engine update (`packages/client/src/lib/compareParameterDiffs.ts`): strategy/drawdown type rows now render human-friendly labels (`Constant Dollar`, `1/N`, `Bucket`, `Rebalancing`, etc.).
  - Compare panel update (`packages/client/src/components/output/CompareParameterDiffTable.tsx`): added Stress Test-style collapsible header with chevron and preserved difference badge.
  - Tests updated (`packages/client/src/lib/compareParameterDiffs.test.ts`): strategy label assertions now target plain-English output and include drawdown label coverage.
  - Canonical docs impact: none (behavior remains within existing v2.1 affordance and semantics).
- CPV30 started: Compare Portfolios v3.0 docs-first wave (always-on compare within Planning/Tracking).
  - Created `docs/features/compare-portfolios-v3-0/{FEATURE,PLAN,ACCEPTANCE}.md`.
  - Added root tracker tasks `CPV30-T1`..`CPV30-T6` in `TASKS.md`.
  - Locked major-scope assumptions: remove Compare mode contract, slot-count-based compare activation, tracking canonical-floor from slot `A`, and snapshot hard-break for legacy compare mode payloads.
- CPV30-T1 complete.
  - v3.0 feature docs are decision-complete for implementation slices and acceptance.
- CPV30 implementation complete for root tasks `CPV30-T2`..`CPV30-T6`.
  - Breaking mode-contract update:
    - Removed `AppMode.Compare` from `packages/shared/src/constants/enums.ts`.
    - `SimulationConfig.mode` now effectively `planning|tracking` in shared schemas/contracts.
  - Store/model refactor (`packages/client/src/store/useAppStore.ts`):
    - Compare activation is slot-count-based (`slotOrder.length > 1`) via `useIsCompareActive`.
    - Compare workspace minimum slot count is now 1 (`A` only default).
    - Added tracking canonical-floor normalization: non-`A` overrides at/before `A.lastEditedMonthIndex` are replaced by canonical `A` overrides.
    - Enforced `A` non-removability with min-1 guard and updated compare config mode mapping to current app mode.
  - UI decoupling from mode:
    - `packages/client/src/components/layout/CommandBar.tsx`: mode toggle is now Planning/Tracking only; multi-slot run path keyed off compare-active slot count; tracking compare run now sends slot-specific `actualOverridesByMonth`.
    - `packages/client/src/components/layout/Sidebar.tsx`: compare slot manager always visible; remove affordance now shown only when slot count > 1 and slot != `A`.
    - Output components now route single vs compare by slot count, not mode:
      - `packages/client/src/components/output/SummaryStatsBar.tsx`
      - `packages/client/src/components/output/PortfolioChart.tsx`
      - `packages/client/src/components/output/DetailTable.tsx`
      - `packages/client/src/components/output/StressTestPanel.tsx`
      - `packages/client/src/components/output/CompareParameterDiffTable.tsx`
    - Detail table compare tabs now support tracking edits for active non-`A` slots only after canonical boundary; pre-boundary cells are locked/read-only.
  - Snapshot migration (`packages/client/src/store/snapshot.ts`):
    - Bumped `SNAPSHOT_SCHEMA_VERSION` to `5`.
    - Added explicit rejection of legacy snapshots containing `data.mode = "compare"`.
    - Updated compare workspace unpack defaults/validation to allow 1..8 slot order.
  - Test updates:
    - `packages/client/src/store/useAppStore.compare.test.ts` updated for A-only initialization and min-1 behavior.
    - `packages/client/src/store/snapshot.test.ts` added explicit legacy compare-mode rejection coverage.
    - `packages/client/src/lib/compareParameterDiffs.test.ts` updated to use non-compare app mode value.
  - Canonical docs updated:
    - `docs/SPECS.md`
    - `docs/SCENARIOS.md`
    - `docs/DATA_MODEL.md`
    - `docs/API.md`
    - `docs/ARCHITECTURE.md`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- CPV30 follow-up defect fix: tracking detail-ledger editability + expand control parity in compare-active view.
  - Restored spreadsheet expand/compress button in compare-active `DetailTable` header.
  - Added inline `Edit` affordances for editable cells in compare-active tracking monthly ledger.
  - Added compare-active reset column parity (`↺`) with canonical-boundary lock behavior for non-`A` slots.
  - Preserved keyboard Enter-to-edit flow for unlocked editable cells.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm run test -w @finapp/client -- DetailTable`
- CPV30 defect follow-up (tracking edit commit + stale-state UX + MC manual-fallback consistency).
  - Fixed Tracking ledger edit commit behavior in `DetailTable` by preventing input key event bubbling and adding compare-active `Enter` guard (`editingCell === null`) to stop re-opening edit mode while committing.
  - Added `Tab` commit-navigation parity and input focus-select behavior in compare-active edit cells.
  - Broadened Tracking stale affordance visibility to both Manual and Monte Carlo in `DetailTable` and `PortfolioChart` (badge/message + chart dim state).
  - Updated `useActiveSimulationResult` to fallback to manual cache when MC cache is absent, preventing partial-output blanks immediately after Manual -> MC mode switch prior to rerun.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm test -w @finapp/client`
- `npm run build -w @finapp/client`
- CPV30 UX consistency pass: unified Detail Ledger edit affordance and interaction parity between single-slot and compare-active paths.
  - Compare-active cells now use clickable `(edit)` affordance (pointer cursor) matching single-slot path.
  - Clicking `(edit)` enters inline edit mode directly in both paths.
  - Compare-active edited-cell indicator now uses same blue-dot token as single path.
  - Reduced compare-active ledger compute scope to active slot rows only (instead of computing all slot row tables every render) to improve responsiveness.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm test -w @finapp/client`
- CPV30 compare diff-table flicker fix: kept `CompareParameterDiffTable` mounted during reruns by removing run-status gating (`simulationStatus === 'running'`) and per-slot `status === 'complete'` requirement; table now renders from latest available per-slot `configSnapshot` while simulation is in-flight.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- CPV30 defect fix: `Clear Actuals` now clears value effects (not just edited styling) in Tracking ledger.
  - Fixed root cause where tracking reforecast updates were overwriting `simulationResults.manual`; manual baseline is now preserved and incremental tracking updates write to `simulationResults.reforecast`.
  - Updated explicit run path in tracking-with-overrides: Manual writes merged result to `reforecast`; Monte Carlo remains mode cache update.
  - Updated clear actions to drop `simulationResults.reforecast` when no overrides remain, preventing stale override-driven rows from persisting after clear.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm test -w @finapp/client`
- CPV30 defect fix: ensured source slot workspace is always persisted during `addCompareSlotFromSource` activation path.
  - Fixed case where first compare activation could create slot `B` without materializing slot `A` in `compareWorkspace.slots`, causing Portfolio A to disappear from compare output rendering.
  - Added store test assertion that both `slots.A` and `slots.B` are defined after first activation (`A` + `B`).

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm run test -w @finapp/client -- useAppStore.compare.test.ts`
- CPV30 defect fix: canonical-floor propagation now handles A-boundary shrink/clear correctly.
  - Added non-A canonical-range pruning when active slot `A` clears/shrinks overrides; this removes previously propagated A-locked months from `B..H` before re-normalization.
  - Updated tracking override mutators to persist active-slot overrides into compare workspace and normalize canonical floor immediately.
  - Added regression test: clearing an A override removes propagated value from B for the same locked month.

Validation run (client passing):
- `npm run test -w @finapp/client -- useAppStore.compare.test.ts`
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- CPV30 regression fix: Stress Test effect no longer self-restarts on compare stress status updates.
  - Root cause: `StressTestPanel` effect depended on cloned `compareWorkspace.slotOrder`/`slots` objects; stress status/result writes clone compare workspace, retriggering effect and aborting in-flight stress runs repeatedly.
  - Fix: replaced dependency on cloned compare workspace structures with stable `compareSlotOrderKey` + existing primitive deps, preventing infinite rerun/abort loop.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- CPV30 compare-stress UX fix: Stress Test metrics table is now compare-aware.
  - In compare-active mode, table columns now map to `Portfolio A/B/...` instead of generic single-run columns (`Base`, `Scenario A`, etc.).
  - Added compare rows for base metrics per slot and scenario deltas per slot, so users can compare stress impact across portfolios directly.
  - Single-portfolio stress table behavior remains unchanged.

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- [BUG-0002] Started defect lane: compare stress table spec-parity restoration.
  - Created docs/issues artifacts:
    - `docs/issues/BUG-0002-compare-stress-table-spec-parity/ISSUE.md`
    - `docs/issues/BUG-0002-compare-stress-table-spec-parity/PLAN.md`
    - `docs/issues/BUG-0002-compare-stress-table-spec-parity/ACCEPTANCE.md`
  - Appended BUG-0002 index/tracker entries in `docs/issues/INDEX.md` and `TASKS.md`.
  - Canonical docs impact assumption: none (implementation correction to existing specs #59/#71).
- [BUG-0002] Completed: compare stress table spec-parity restoration.
  - Added pure compare stress table model builder: `packages/client/src/lib/compareStressTableModel.ts`.
  - Added unit coverage: `packages/client/src/lib/compareStressTableModel.test.ts`.
  - Refactored compare table rendering in `packages/client/src/components/output/StressTestPanel.tsx` to sectioned model:
    - sections: Base + Scenario labels from shared scenario config
    - per-slot columns (`Portfolio A..H`) with slot color dots
    - full base and scenario metric rows, including Median/Mean Monthly Withdrawal (Real)
    - MC-only rows (`Probability of Success`, `Δ Success vs. Base`) in both base/scenario sections
    - scenario resolution by `scenarioId` with `—` fallback for sparse/missing slot results
  - Single-slot stress table branch left intact (no behavior change).
  - Defect docs/tracking updated:
    - `docs/issues/BUG-0002-compare-stress-table-spec-parity/{ISSUE.md,PLAN.md,ACCEPTANCE.md}`
    - `docs/issues/INDEX.md`
    - `TASKS.md`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- None. Implementation correction to existing stress specs (#59, #71).

## 2026-02-24 — DETAIL-LEDGER-SPREADSHEET | COMPLETE

Decomposed 1287-line DetailTable.tsx monolith into 13 focused files under `DetailLedger/`:
- TanStack Table + TanStack Virtual replaces hand-rolled virtualization
- Cell-level React.memo with custom comparator for surgical re-renders
- Keyboard navigation state machine: arrow keys, Tab, Enter, Escape, type-to-edit, Cmd+C/V
- Coordinate-based focus management via ref registry
- Unified code path (normal = single-slot compare)
- Non-blocking reforecast with 500ms debounce (up from 250ms)
- All editing rules preserved (Tracking Mode constraints, canonical boundary lock, validation)

New files: cellHelpers.ts, useDetailRows.ts, useDetailColumns.ts, useGridNavigation.ts, useReforecast.ts, CellEditor.tsx, DetailCell.tsx, DetailRow.tsx, VirtualizedBody.tsx, CompareSlotTabs.tsx, DetailLedgerToolbar.tsx, DetailLedgerContainer.tsx, index.ts

Deleted: DetailTable.tsx (old monolith)
Bundle size reduced: 510KB → 489KB (gzip 137KB → 133KB)

Validation passed: `npm run typecheck && npm run lint && npm run test && npm run build`

[DECISION] TanStack Table + Virtual now canonical table library (was "prescribed but not installed")
[DECISION] Keyboard navigation uses coordinate-based state machine, not DOM queries


## 2026-02-25 12:10 — DOC-HYGIENE-TANSTACK-DRIFT | COMPLETE

Removed phantom `@tanstack/react-table` dependency from client package and synchronized root lockfile via `npm install`.
Corrected TanStack Table doc drift across architecture/engineering/detail-ledger feature docs; Detail Ledger is now documented as TanStack Virtual + hand-rolled sorting/column visibility.

Validation run (client):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`

## 2026-02-25 00:18 — TRACKING-LEDGER-CONTRACT-v1.0 | COMPLETE

Implemented Tracking Detail Ledger Contract v1.0 with Slot `A` canonical actuals and stale-first rerun behavior.

- [TLC-T1] Created feature docs:
  - `docs/features/tracking-ledger-contract/FEATURE.md`
  - `docs/features/tracking-ledger-contract/PLAN.md`
  - `docs/features/tracking-ledger-contract/ACCEPTANCE.md`
- [TLC-T2/T3] Enforced Tracking editability contract in ledger helpers/navigation:
  - Editable columns reduced to start balances + withdrawal-by-asset only.
  - Non-`A` compare slots blocked from ledger edits.
  - Month edit guard added (`currentMonthIndex + 1` upper bound).
- [TLC-T4] Added same-row derived congruence coverage:
  - `packages/client/src/components/output/DetailLedger/cellHelpers.test.ts`
  - `packages/client/src/lib/trackingActuals.test.ts`
- [TLC-T5] Applied stale-first lifecycle for Tracking (Manual + Monte Carlo):
  - Tracking ledger and input changes mark outputs stale.
  - Explicit Run Simulation clears stale and refreshes outputs.
- [TLC-T6] Canonicalized compare Tracking runtime to Slot `A` actual history:
  - Non-`A` overrides blocked/ignored for effective run behavior.
  - Compare run and stress paths consume canonical tracking override map.
- [TLC-T7] Kept snapshot compatibility:
  - Legacy override fields/schema accepted.
  - Runtime ignores legacy per-row income/expense override authoring path.
- [TLC-T8] Updated canonical docs:
  - `docs/SPECS.md`
  - `docs/SCENARIOS.md`
  - `docs/ARCHITECTURE.md`
  - `docs/DATA_MODEL.md`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Tracking edit contract is now explicit and aligned across product, scenario, architecture, and data-model docs.
- No backend API contract changes.

## 2026-02-27 14:42 — Detail Ledger UX Alignment (Tracking Monthly)

Applied a focused UX pass for Tracking detail ledger consistency and readability.

- Normalized Monthly virtualization sizing to shared constants (`rowHeight`/`overscan`) so density behavior is deterministic.
- Added compact editable affordance for Slot `A` cells using theme-aware dotted underline (no inline `(edit)` text).
- Updated non-`A` locked-cell presentation to mild desaturate + opacity while preserving red/green semantic value colors.
- Neutralized stale-state presentation to theme-congruent dimming for chart/stats/table surfaces.
- Fixed Slot `A` monthly row inflation by compacting the `Reset` column cell/button sizing so row height matches other slots after mode/slot switching.

Validation run (passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`

Canonical docs impact:
- No canonical doc impact (implementation-level UX refinements only).

## 2026-02-27 — Feature: Withdrawal Chart (Side-by-Side) | COMPLETE

Implemented a new Withdrawal Chart rendered side-by-side with the existing Portfolio Chart inside a shared ChartPanel container.

New files:
- `packages/client/src/lib/chartPrimitives.ts` — shared chart utilities (`linePath`, `areaPath`, `inflationFactor`, constants, coordinate helpers)
- `packages/client/src/components/output/WithdrawalChart.tsx` — withdrawal chart with all rendering modes (single, breakdown, compare, MC representative path, stress, tracking)
- `packages/client/src/components/output/ChartPanel.tsx` — wrapper owning ResizeObserver, shared hoverIndex, Nominal/Real + Breakdown toggles, stale/loading overlays, responsive layout
- `docs/features/withdrawal-chart/` — FEATURE.md, PLAN.md, ACCEPTANCE.md

Modified files:
- `PortfolioChart.tsx` — refactored to accept `hoverIndex`, `onHoverChange`, `chartWidth` props; shed section wrapper, toggles, ResizeObserver, overlays; imports from chartPrimitives
- `AppShell.tsx` — replaced `<PortfolioChart />` with `<ChartPanel />`
- `TASKS.md` — added WC-T1 through WC-T6

Key behaviors:
- Synchronized hover crosshair across both charts
- Shared Nominal/Real and Breakdown toggles (Breakdown hidden in compare mode)
- Compare mode: one line per slot with matching "Portfolio X" labels
- MC mode: representative path only (no percentile bands) with annotation
- Stress overlays: dashed withdrawal lines (hidden when breakdown enabled)
- Tracking: actuals boundary line, stale dimming at panel level
- Responsive: side-by-side >= 900px, vertical stack below
- Shortfall tooltip when actual < requested

Validation run (passing):
- `npm run typecheck` — clean
- `npm run lint` — clean
- `npm test` — 105/105 passed
- `npm run build` — successful

Canonical docs impact:
- `docs/SPECS.md` — added Affordance #74 (ChartPanel) and #75 (WithdrawalChart)
- `docs/ARCHITECTURE.md` — updated component tree and file listing
- `docs/SCENARIOS.md` — added Scenario #27 (side-by-side chart viewing)

## 2026-02-28 — Feature: Bookmarks (Local Compressed Persistence) | COMPLETE

Implemented command-bar bookmarks as fast in-browser persistence, separate from snapshot file save/load.

Feature docs:
- `docs/features/bookmarks/FEATURE.md`
- `docs/features/bookmarks/PLAN.md`
- `docs/features/bookmarks/ACCEPTANCE.md`

Implementation:
- Added bookmark persistence module: `packages/client/src/store/bookmarks.ts`
  - Types: `BookmarkRecord`, `BookmarksStorageEnvelope`
  - Storage key/version: `finapp:bookmarks:v1`
  - Full-state payload reuse via snapshot serializer/parser
  - Compression: gzip + base64 (`pako`)
  - Policies: newest-first insert, duplicate-name allowed, max 100 bookmarks, oldest eviction, explicit quota error
- Added tests: `packages/client/src/store/bookmarks.test.ts`
  - round-trip restore
  - newest-first ordering
  - duplicate-name allowance
  - targeted delete
  - 101st-save eviction behavior
  - corrupted payload rejection without state mutation
  - envelope version mismatch and quota error behavior
- Updated command bar UX: `packages/client/src/components/layout/CommandBar.tsx`
  - Added `Create Bookmark` button
  - Added Create Bookmark modal (required name; disabled save on empty input)
  - Added Bookmarks dropdown (newest-first, immediate load, hover trash + delete confirm)
  - Reused toolbar message area for bookmark save/load/delete/quota errors
  - Snapshot Save/Load behavior remains intact

Tracker updates:
- Marked stale theme QA tasks complete by user direction:
  - `FM-T4`
  - `FS84-T4`
- Added and completed root tasks:
  - `BM-T1` through `BM-T4`

Additional test maintenance:
- Updated `packages/client/src/store/useAppStore.compare.test.ts` tracking compare month indices to match current sequential edit-window contract.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/PRD.md`
  - `docs/SPECS.md` (new affordances #76 and #77)
  - `docs/SCENARIOS.md` (new scenario #28)
  - `docs/ARCHITECTURE.md`
  - `docs/DATA_MODEL.md`
  - `docs/API.md`

## 2026-02-28 — Bookmark UI Refinement (Icon-Only Create Control) | COMPLETE

Applied a focused command-bar UX refinement for bookmarks.

Changes:
- Replaced text `Create Bookmark` button with icon-only control in `CommandBar`.
- Implemented YouTube-style save/playlist-add visual metaphor (lines + plus) as inline SVG.
- Added hover tooltip text `Create Bookmark` matching existing icon-button tooltip pattern.
- Preserved existing bookmark behavior (modal create flow, newest-first list, immediate load, hover delete with confirm).
- Preserved `Bookmarks` terminology as requested.

Files updated:
- `packages/client/src/components/layout/CommandBar.tsx`
- `docs/SPECS.md` (Affordance #76 updated to icon-only semantics)

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- `docs/SPECS.md` updated (UI affordance presentation detail).
- No API/data model/architecture contract changes.

## 2026-02-28 — Feature: Compare Portfolios v3.1 (Lockable/Syncable Inputs) | COMPLETE

Implemented A-master compare lock/sync behavior for input-panel parameters across slots `A..H`.

Feature docs created:
- `docs/features/compare-portfolios-v3-1-lock-sync/FEATURE.md`
- `docs/features/compare-portfolios-v3-1-lock-sync/PLAN.md`
- `docs/features/compare-portfolios-v3-1-lock-sync/ACCEPTANCE.md`

Implementation highlights:
- Added compare sync model to store under `compareWorkspace.compareSync` with:
  - family locks (`coreParams`, `startingPortfolio`, `returnAssumptions`, `spendingPhases`, `withdrawalStrategy`, `drawdownStrategy`, `incomeEvents`, `expenseEvents`)
  - instance locks for list families (`spendingPhases`, `incomeEvents`, `expenseEvents`)
  - per-slot unsync maps for family and instance overrides
- Added store actions/selectors:
  - `toggleCompareFamilyLock`
  - `toggleCompareInstanceLock`
  - `setCompareSlotFamilySync`
  - `setCompareSlotInstanceSync`
  - UI selectors for family/instance lock/read-only state
- Added lock propagation engine (`A` as master):
  - family-level propagation to synced followers
  - list family global lock exact mirror behavior
  - list instance lock merge-by-id behavior
  - locked instance delete propagation from `A` to synced followers
- Added follower-side edit guards for locked+synced families/instances across all input mutation paths.
- Added UI lock/sync controls:
  - section-level controls in sidebar collapse headers
  - per-instance controls on Spending Phase, Income Event, Expense Event cards
  - synced followers render read-only controls while unsynced followers are editable
- Extended shared input components with disabled support where needed.

Persistence updates:
- Snapshot schema version bumped: `SNAPSHOT_SCHEMA_VERSION = 6`.
- Snapshot pack/unpack includes `compareSync` and defaults missing `compareSync` for older payloads.
- Bookmark persistence round-trips `compareSync` via snapshot payload path.

Tests added/updated:
- `packages/client/src/store/useAppStore.compare.test.ts`
  - family lock sync/unsync/resync
  - global list lock exact mirror + edit block
  - instance lock merge semantics + delete propagation
- `packages/client/src/store/snapshot.test.ts`
  - compareSync persistence
  - missing compareSync backward default
- `packages/client/src/store/bookmarks.test.ts`
  - compareSync bookmark round-trip

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/SPECS.md` (new Affordance #78)
  - `docs/SCENARIOS.md` (new Scenario #29)
  - `docs/DATA_MODEL.md` (compareWorkspace v3.1 + compareSync model)
  - `docs/ARCHITECTURE.md` (compare sync propagation architecture)
- No API contract changes (`docs/API.md` unchanged).

## 2026-02-28 — CPV31 Hotfix: Prevent Lock-UI Render Loop | COMPLETE

Fixed browser runtime crash (`Maximum update depth exceeded`) introduced during compare lock/sync UI wiring.

Root cause:
- Lock UI hooks selected object literals from Zustand store in a way that could trigger unstable re-subscription/render behavior in this setup.

Fix:
- Reworked `useCompareFamilyLockUiState` and `useCompareInstanceLockUiState` to use primitive selectors and compose return values locally.
- Removed unsupported equality-arg usage on `useAppStore` selector calls.

File updated:
- `packages/client/src/store/useAppStore.ts`

Validation run (client passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm test -w @finapp/client`

## 2026-02-28 — CPV31 Follow-up: Sequential Spending Phase Instance Locks | COMPLETE

Implemented a targeted CPV31 follow-up to prevent non-sequential Spending Phase instance locks from creating phase gaps in follower slots.

Feature update docs created:
- `docs/features/compare-portfolios-v3-1-lock-sync-v1-1/FEATURE.md`
- `docs/features/compare-portfolios-v3-1-lock-sync-v1-1/PLAN.md`
- `docs/features/compare-portfolios-v3-1-lock-sync-v1-1/ACCEPTANCE.md`

Implementation:
- Added Spending Phase lock-prefix helpers in compare-sync engine:
  - normalized lock set must be a contiguous prefix from Phase 1
  - lock eligibility requires prior phase locked (except first phase)
  - unlock cascades to all later locked phases
- Applied normalization in compare-sync propagation/runtime normalization path so invalid legacy lock sets are auto-repaired.
- Updated `toggleCompareInstanceLock('spendingPhases', ...)` to enforce eligibility and cascade-unlock tail behavior.
- Added Slot `A` UI affordance:
  - Spending phase lock icon is disabled when phase is not eligible to lock
  - tooltip explains requirement (`Lock prior phase first`)
- No behavior change for Income/Expense instance locks.

Tests added/updated:
- `packages/client/src/store/useAppStore.compare.test.ts`
  - sequential lock eligibility
  - lock progression 1->2->3
  - cascade unlock behavior
- `packages/client/src/store/snapshot.test.ts`
  - non-prefix spending lock payload normalization on load

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/SPECS.md`
  - `docs/SCENARIOS.md`
  - `docs/DATA_MODEL.md`
  - `docs/ARCHITECTURE.md`
- No API contract changes.
2026-02-28 — CPV311-T4 Spending lock eligibility refresh + single-slot lock visibility | COMPLETE
- Fixed Spending Phase instance lock eligibility UI in Slot A to use the live rendered phase list (`state.spendingPhases`), which resolves false-disabled lock icons on newly added phases when the prior phase is locked.
- Updated compare lock control rendering to hide lock/sync icons whenever compare has only one slot (`A`).
- Files changed:
  - `packages/client/src/store/useAppStore.ts`
  - `packages/client/src/components/shared/CompareSyncControl.tsx`
  - `TASKS.md`
- Validation run (all passing):
  - `npm run typecheck -w @finapp/client`
  - `npm run lint -w @finapp/client`
  - `npm test -w @finapp/client`
- Canonical docs impact: No canonical doc impact (bugfix + visibility polish within existing CPV31.1 behavior contract).
## 2026-02-28 — TEG11-T1/T2/T3/T4 | COMPLETE

Implemented Theme Engine Granular v1.1 with token model v2 (`semantic -> slots -> overrides`) and slot catalog delivery from `/api/v1/themes`. Added server-side token-model validation and client-side slot resolver/compiler that emits `--theme-slot-*` variables during theme application.

Migrated key UI surfaces to slot-driven classes (app shell, command bar, section chrome, shared controls, summary stats, compare diff panel, chart panel, stress panel) while retaining existing startup precedence and snapshot/local preference behavior. Snapshot parsing now defaults missing `theme.slotCatalog` to `[]` for backward compatibility.

Updated docs and feature artifacts:
- `docs/features/theme-engine-granular-v1-1/{FEATURE,PLAN,ACCEPTANCE}.md`
- `docs/SPECS.md`
- `docs/DATA_MODEL.md`
- `docs/API.md`
- `docs/ARCHITECTURE.md`
- `TASKS.md`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
2026-02-28 — CPV31.2 Zero-Phase Spending Defaults | COMPLETE
- Implemented optional Spending Phases (`0..4`) with zero-phase default behavior.
- Shared contract updated: `spendingPhases` now accepts empty arrays (`max(4)` only).
- Engine behavior updated in deterministic + Monte Carlo simulation paths:
  - empty phase list no longer throws
  - phase min/max clamp is applied only when an active/fallback phase exists
- Client store updated:
  - initial state now uses `spendingPhases: []`
  - removing the last phase returns to empty state
  - missing-id remove is a no-op
- Spending Phases UI now renders an empty-state explanation + Add CTA when no phases exist.
- Snapshot/bookmark load migration implemented:
  - loaded root/planning/tracking/compare workspaces are normalized to zero spending phases
  - spending-phase compare instance locks/unsync overrides are cleared during load normalization
- Added feature wave docs:
  - `docs/features/compare-portfolios-v3-1-lock-sync-v1-2/FEATURE.md`
  - `docs/features/compare-portfolios-v3-1-lock-sync-v1-2/PLAN.md`
  - `docs/features/compare-portfolios-v3-1-lock-sync-v1-2/ACCEPTANCE.md`
- Tests added/updated:
  - client store/snapshot/bookmark coverage for zero-phase defaults and load migration
  - server deterministic + monte carlo zero-phase no-clamp parity tests
  - route test for `/simulate` with empty spending phases

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/SPECS.md`
  - `docs/SCENARIOS.md`
  - `docs/DATA_MODEL.md`
  - `docs/ARCHITECTURE.md`
  - `docs/API.md`
2026-02-28 — STC-T1 Stay The Course theme pack docs | COMPLETE
- Created new theme feature pack under:
  - `docs/features/themes/stay-the-course/FEATURE.md`
  - `docs/features/themes/stay-the-course/PLAN.md`
  - `docs/features/themes/stay-the-course/ACCEPTANCE.md`
  - `docs/features/themes/stay-the-course/TOKENS.md`
- Captured authoritative dark palette and mapped it to token/semantic intent for implementation.
- Updated root tracker:
  - `TASKS.md` (added/completed `STC-T1`)
- Canonical docs impact: No canonical doc impact (docs-only theme-pack creation; no contract/behavior changes implemented yet).
2026-02-28 — STC-T2 Stay The Course built-in theme implementation | COMPLETE
- Added shared enum ID:
  - `packages/shared/src/constants/enums.ts` (`ThemeId.StayTheCourse = 'stayTheCourse'`)
- Added server built-in theme definition in:
  - `packages/server/src/themes/registry.ts`
  - dark palette anchored to `#0C0C0C` with teal-forward accents from provided colors
- Updated route test built-in ID assertions:
  - `packages/server/tests/routes/themes.test.ts`
- Updated root tracker:
  - `TASKS.md` (added/completed `STC-T2`)

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact: No canonical doc impact (additive built-in theme within existing theme model/API shape).
2026-02-28 — TFLD1 Theme Families + Per-Theme Appearance (Light/Dark) v1 | COMPLETE
- Implemented shared theme model migration from flat theme IDs to family + appearance selection:
  - added `ThemeFamilyId`, `ThemeAppearance`, `ThemeVariantId`
  - `ThemeDefinition` now includes `familyId` and `appearance`
  - `/themes` response contract now includes `defaultSelection`, `families`, and `variants`
- Implemented server theme registry grouped output with compatibility aliases:
  - canonical payload fields: `defaultSelection`, `families`, `variants`
  - legacy compatibility fields retained: `defaultThemeId`, `themes`, `catalog`
  - server route tests expanded for family/variant coverage
- Implemented client migration:
  - store theme slice now tracks selected family, per-family remembered appearance, and active variant id
  - startup precedence preserved: snapshot > local preference > server default
  - local preference migrated to `finapp:theme-selection-v1` with legacy `finapp:theme-id` fallback mapping
  - command bar theme popover now renders family rows with light/dark toggles (A11y family remains dark-only badge)
- Implemented snapshot compatibility migration:
  - legacy snapshot fields (`selectedThemeId`/`defaultThemeId`) map deterministically to family+appearance
  - new theme fields are normalized on load
- Added feature wave docs:
  - `docs/features/theme-families-light-dark-v1/FEATURE.md`
  - `docs/features/theme-families-light-dark-v1/PLAN.md`
  - `docs/features/theme-families-light-dark-v1/ACCEPTANCE.md`
- Updated root tracker:
  - `TASKS.md` (added/completed `TFLD1-T1..TFLD1-T4`)

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/SPECS.md`
  - `docs/DATA_MODEL.md`
  - `docs/API.md`
  - `docs/ARCHITECTURE.md`
2026-02-28 — TFLD1 follow-up: compare-slot contrast tuning for light variants + Stay The Course differentiation | COMPLETE
- Addressed UX feedback on compare slot visibility and chart-line separation:
  - Improved light-variant compare-slot contrast by enforcing a darker `compareSlotA` and a high-contrast spread for `compareSlotC..H` in generated light variants.
  - Preserved family identity in light variants via family accent on `compareSlotB`.
  - Reworked Stay The Course compare-slot palette (dark variant) to include additional distinct accent hues beyond narrow teal shades for clearer line/chip differentiation.
- Updated theme authoring guidance:
  - `docs/features/themes/GUIDE.md` now explicitly requires compare-slot palette distinctness and allows adding supplemental accents when needed.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/features/themes/GUIDE.md`

2026-02-28 — MT-T1: Add 40+ meme theme families to shared enums | COMPLETE
- Added 40 new ThemeFamilyId entries to enums.ts:
  - PatagoniaVest, MoneyNeverSleeps, Hodl, TrustMeBro, ThreePieceSuit
  - BuyHighSellLow, ExitStrategy, Ath, Coastin, MarginCall, BigShort
  - HoldingTheBag, Boglehead, CryptoBro, TheVillages, SeedRound
  - Nineteen87, Ramen, ToTheMoon, Wagmi, Ngmi, BuyTheDip
  - ThisIsFine, DeadCatBounce, RugPull, Whale, LamboLoading
  - TheSingularity, ZoomOutBro, BlackSwan, TulipMania, Nineteen29Vibes
  - DotComShuffle, BeanieBaby, DebtSnowball, SirThisIsAWendys
  - Hopium, Copium, BaristaFire, ThisTimeItsDifferent, Satoshi
- Added 80 new ThemeVariantId entries (light + dark for each family)
- Updated client store defaults in useAppStore.ts and snapshot.ts

2026-02-28 — MT-T2: Implement server registry with dark variants | COMPLETE
- Added 40 dark theme variants with unique color tokens to registry.ts
- Each theme has distinctive palette matching its name/personality
- Light variants auto-generated preserving family accent colors
- Added family definitions with descriptions

2026-02-28 — MT-T3: Complete canonical docs updates | COMPLETE
- Created feature folder docs/features/meme-themes-v1/
- Created FEATURE.md, PLAN.md, ACCEPTANCE.md
- Updated TASKS.md with MT-T1, MT-T2, MT-T3

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Created:
  - `docs/features/meme-themes-v1/FEATURE.md`
  - `docs/features/meme-themes-v1/PLAN.md`
  - `docs/features/meme-themes-v1/ACCEPTANCE.md`
- Updated:
  - `TASKS.md`

2026-02-28 — CHG-0005: Theme dropdown scroll + selected-row visibility | COMPLETE
- Added bounded scroll behavior to command bar theme popover list (`max-h-80 overflow-y-auto`).
- Added selected-row ref + open-time auto-scroll (`scrollIntoView({ block: 'nearest' })`) so reopening the menu keeps the current theme visible.
- Preserved existing theme family selection and light/dark appearance toggle behavior.

Validation run (passing):
- `npm run typecheck`
- `npm run lint`

Canonical docs impact:
- No canonical doc impact.
- Updated minor-change lane artifacts only:
  - `docs/changes/INDEX.md`
  - `docs/changes/CHG-0005-theme-dropdown-scroll-selected-visibility/CHANGE.md`
  - `docs/changes/CHG-0005-theme-dropdown-scroll-selected-visibility/ACCEPTANCE.md`
  - `TASKS.md`

2026-02-28 — CHG-0005-T3: Theme popover scrollbar tokenization | COMPLETE
- Added command bar scrollbar theme slots in `index.css` for track/thumb/hover colors.
- Added `.theme-commandbar-scrollbar` cross-browser scrollbar styling (`scrollbar-color` + WebKit pseudo-elements).
- Applied themed scrollbar class to both scrollable command bar popovers (Bookmarks and Theme) to eliminate bright white bars in dark themes.

Validation run (passing):
- `npm run typecheck`
- `npm run lint`

Canonical docs impact:
- No canonical doc impact.
- Updated minor-change artifacts:
  - `TASKS.md`
  - `docs/changes/CHG-0005-theme-dropdown-scroll-selected-visibility/CHANGE.md`
  - `docs/changes/CHG-0005-theme-dropdown-scroll-selected-visibility/ACCEPTANCE.md`

2026-03-01 — TFLD11-T1/T2/T3/T4: Theme Families v1.1 replacement wave | COMPLETE
- Created feature update docs:
  - `docs/features/theme-families-light-dark-v1-1/FEATURE.md`
  - `docs/features/theme-families-light-dark-v1-1/PLAN.md`
  - `docs/features/theme-families-light-dark-v1-1/ACCEPTANCE.md`
- Replaced user-facing legacy families while preserving stable IDs:
  - `ThemeFamilyId.Monokai` / `ThemeVariantId.Monokai*` now render as **Circuit Breaker**.
  - `ThemeFamilyId.Synthwave84` / `ThemeVariantId.Synthwave84*` now render as **Powell Pivot**.
- Updated dark variant token bundles in `packages/server/src/themes/registry.ts` for both replacement families using the approved palettes.
- Added targeted post-generation light overrides for `MonokaiLight` and `Synthwave84Light` so replacement light variants are distinct (non-generic) while retaining stable IDs.
- Updated family metadata and legacy catalog labels in `/api/v1/themes` response payload to replacement names/descriptions.
- Extended route tests (`packages/server/tests/routes/themes.test.ts`) to verify:
  - replacement names present,
  - old Monokai/Synthwave labels absent from active family names,
  - both replacement families still expose light+dark appearances,
  - legacy catalog IDs remain intact.
- Updated canonical UX doc list in `docs/SPECS.md` Affordance #66 to `Circuit Breaker` and `Powell Pivot`.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated:
  - `docs/SPECS.md`
- No canonical contract impact:
  - `docs/API.md` (unchanged)
  - `docs/DATA_MODEL.md` (unchanged)
  - `docs/ARCHITECTURE.md` (unchanged)
- `docs/SCENARIOS.md` unchanged (no named-family references required for this wave).

2026-03-01 — CHG-0006: Segmented toggle active-hover contrast fix | COMPLETE
- Fixed segmented toggle hover specificity so active option retains active fill/text while hovered.
- Added explicit `.theme-segmented-option-active:hover` rule in shared client theme CSS.
- This resolves Planning/Tracking (and other segmented controls) hover regressions in light and dark themes.

Validation run (passing):
- `npm run typecheck`
- `npm run lint`

Canonical docs impact:
- No canonical doc impact.
- Updated minor-change artifacts:
  - `docs/changes/INDEX.md`
  - `docs/changes/CHG-0006-segmented-toggle-active-hover-contrast/CHANGE.md`
  - `docs/changes/CHG-0006-segmented-toggle-active-hover-contrast/ACCEPTANCE.md`
  - `TASKS.md`

2026-03-01 — CHG-0007: Theme menu order + outside-click close | COMPLETE
- Updated command bar theme family ordering with pinned top priorities:
  1) Default, 2) High Contrast, 3) Money Never Sleeps, 4) Patagonia Vest, 5) Stay The Course.
- Added stable pseudo-random ordering for remaining theme families.
- Added outside-click/tap dismissal for command bar Theme and Bookmarks popover menus.
- Kept in-menu interactions intact (selection/toggles/delete actions continue to work).

Validation run (passing):
- `npm run typecheck`
- `npm run lint`

Canonical docs impact:
- No canonical doc impact.
- Updated minor-change artifacts:
  - `docs/changes/INDEX.md`
  - `docs/changes/CHG-0007-theme-menu-order-and-outside-close/CHANGE.md`
  - `docs/changes/CHG-0007-theme-menu-order-and-outside-close/ACCEPTANCE.md`
  - `TASKS.md`

## 2026-03-01 — BUGFIX: Bookmark/Snapshot Spending Phases Restore | COMPLETE

Fixed a regression where loading bookmarks did not restore `spendingPhases`.

Root cause:
- Snapshot parsing was unconditionally clearing `spendingPhases` from top-level and all workspaces during load.
- Bookmark load uses snapshot parsing internally, so bookmarks inherited the same data-loss behavior.

Implementation:
- Updated `packages/client/src/store/snapshot.ts`:
  - preserve `spendingPhases` for current snapshot payloads.
  - keep legacy migration behavior (clear phases) only when loading snapshots missing `compareSync` in `compareWorkspace`.
- Updated `packages/client/src/store/useAppStore.ts`:
  - removed destructive pre-apply normalization that cleared snapshot spending phases.
- Added/updated regression tests:
  - `packages/client/src/store/snapshot.test.ts`
    - new spending phase round-trip assertion
    - legacy-clearing behavior scoped to missing-`compareSync` snapshots
    - spending phase lock normalization expectation adjusted for preserved phase data
  - `packages/client/src/store/bookmarks.test.ts`
    - new spending phase round-trip assertion through bookmark create/load
    - legacy bookmark payload test now explicitly simulates missing `compareSync`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- No canonical doc impact (bugfix to runtime persistence/restore correctness).

2026-03-01 — CHG-0008: MC p50 reference column theme balance | COMPLETE
- Updated MC `Start Total (p50)` reference column styling to use detail-ledger reference slot tokens.
- Changed tint from generic interactive secondary to:
  - `color-mix(in srgb, var(--theme-slot-detail-ledger-reference-bg) 88%, var(--theme-slot-detail-ledger-shell-bg) 12%)`
- Changed text color to `var(--theme-slot-detail-ledger-reference-text)`.
- Column behavior (values, sorting, tooltip) unchanged.

Validation run (passing):
- `npm run typecheck`
- `npm run lint`

Canonical docs impact:
- No canonical doc impact.
- Updated minor-change artifacts:
  - `docs/changes/INDEX.md`
  - `docs/changes/CHG-0008-mc-p50-reference-column-theme-balance/CHANGE.md`
  - `docs/changes/CHG-0008-mc-p50-reference-column-theme-balance/ACCEPTANCE.md`
  - `TASKS.md`

2026-03-01 — Feature: Adaptive TWR Smoothing | COMPLETE
- Implemented configurable blend smoothing for `dynamicSwrAdaptive` (in-place strategy update).
- Added new strategy params across shared contracts, schemas, store, and UI:
  - `smoothingEnabled` (default `true`)
  - `smoothingBlend` (default `0.7`, validated in `[0, 0.95]`)
- Engine behavior updated in deterministic + simulator monthly-cadence paths:
  - compute raw adaptive monthly target
  - optionally blend with prior month final withdrawal (`blend * prior + (1 - blend) * raw`)
  - apply spending phase monthly clamp after smoothing
  - persist final monthly withdrawal as next-month blend anchor
- Added Adaptive TWR strategy tests for smoothing-on/off parity and blend math.
- Added UI controls in strategy panel (smoothing toggle + blend slider) and updated strategy tooltip guidance.
- Extended compare parameter diff model to include smoothing fields.
- Added feature docs:
  - `docs/features/adaptive-twr-smoothing/FEATURE.md`
  - `docs/features/adaptive-twr-smoothing/PLAN.md`
  - `docs/features/adaptive-twr-smoothing/ACCEPTANCE.md`
- Updated canonical docs:
  - `docs/SPECS.md`
  - `docs/WITHDRAWAL_STRATEGIES.md`
  - `docs/DATA_MODEL.md`
  - `docs/API.md`
  - `docs/SCENARIOS.md`

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Yes (strategy parameters, algorithm contract, request shape, and scenario narrative updated).

2026-03-01 — Feature: Sidebar Collapse Toggle | COMPLETE
- Implemented UX affordance to collapse/expand the input sidebar for more horizontal output space.
- Added `isSidebarCollapsed` state and `toggleSidebar` action to `useAppStore` UI slice.
- Updated `AppShell.tsx` layout:
  - Sidebar (`aside`) now transitions its width from `400px` to a `24px` strip (preserving interactive edge).
  - Content within sidebar fades out during collapse.
  - Added a floating circular toggle button with a rotating chevron.
  - Button is vertically aligned with the center of the "Compare Slot" card (~60px from top) for better visibility.
  - Button remains fully accessible on the visible strip when sidebar is collapsed.
  - Main section expands to fill the remaining horizontal space automatically.
- Verified performance and visual transitions:
  - Smooth 300ms CSS transitions for width and opacity.
  - Correct z-index layering to ensure toggle button remains interactive.
- No regressions in layout or responsiveness.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`

Canonical docs impact:
- No canonical doc impact (purely UX enhancement).

2026-03-01 — UX: Consolidate Historical Era Selection | COMPLETE
- Moved "Historical Era" dropdown from navbar (`CommandBar.tsx`) to the "Historical Data" component (`HistoricalDataSummary.tsx`) in the sidebar.
- Enabled per-portfolio historical era selection in Compare mode by making the state slot-aware.
- Added `historicalEra` to `COMPARE_SYNC_FAMILIES`, allowing users to lock or sync eras across all portfolios.
- Co-located historical data fetching logic with the selection UI for better encapsulation.
- Ensured the summary statistics card remains intact and reactive to era selection changes.
- Simplified navbar by removing redundant simulation-mode-specific controls.
- Verified smooth integration and data reactivity in Monte Carlo mode across multiple slots.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`

2026-03-01 — CHG-0009: Bookmark Description Field | COMPLETE
- Added optional `description` field to `BookmarkRecord` type in `bookmarks.ts`
- Updated `createBookmark()` to accept optional description parameter
- Added description input field to Create Bookmark modal in `CommandBar.tsx`
- Description displays below timestamp in dropdown with `·` separator
- Tooltip on hover shows full description
- Added 3 tests for description functionality (save/retrieve, backwards compat, whitespace trim)
- All validation passing: typecheck, lint, test, build

Canonical docs impact:
- No canonical doc impact (backwards compatible optional annotation).

2026-03-01 — Feature: Block Bootstrap Sampling | COMPLETE
- Added `blockBootstrapEnabled` and `blockBootstrapLength` to `SimulationConfig` and Zod schema
- Implemented `sampleHistoricalReturnsBlock` in `monteCarlo.ts` (contiguous blocks with circular wrap)
- Updated call site to dispatch between i.i.d. and block sampling based on config
- Added 3 new MC tests: determinism, divergence from i.i.d., blockLength=1 edge case
- Added fields to WorkspaceSnapshot, SnapshotState, all store helpers, and compare sync (historicalEra family)
- Added `setBlockBootstrapEnabled`/`setBlockBootstrapLength` actions mirroring `setSelectedHistoricalEra` pattern
- Bumped snapshot schema version from 6 to 7
- Added toggle + slider UI in HistoricalDataSummary (between era dropdown and stats table)
- Slider uses `accent-brand-blue` for theme alignment (matching Withdrawal Smoothing slider)
- Fixed `useSimulationConfig` hook to include block bootstrap fields
- All validation passing: typecheck, test (179 tests), build

Feature docs: `docs/features/block-bootstrap/` (FEATURE.md, PLAN.md, ACCEPTANCE.md)

Canonical docs impact:
- `docs/SPECS.md` — added Affordance #11b (Block Bootstrap Sampling Controls)
- `docs/DATA_MODEL.md` — added fields to SimulationConfig
- `docs/API.md` — added block bootstrap config description to POST /api/v1/simulate
- `docs/ARCHITECTURE.md` — updated MC sampling description with block bootstrap mode

2026-03-02 — Feature: Historical Era Custom Range (Month-Year) | COMPLETE
- Added `HistoricalEra.Custom` and `customHistoricalRange` to shared simulation contracts.
- Extended Zod simulation schema validation:
  - `customHistoricalRange` is required when `selectedHistoricalEra = custom`.
  - custom range enforces month/year validity and `start <= end` ordering.
- Refactored server historical filtering into shared month-year resolver used by both:
  - Monte Carlo sampling source rows, and
  - `GET /api/v1/historical/summary` responses.
- Extended historical summary route query contract to support custom range params:
  - `era=custom&startMonth&startYear&endMonth&endYear`.
- Added client store/snapshot wiring for `customHistoricalRange`:
  - Included in `AppStore`, `WorkspaceSnapshot`, `SnapshotState`, workspace/config helpers, and compare sync propagation under `historicalEra` family.
  - Added `setCustomHistoricalRange` action with tracking stale-state behavior and compare lock/sync semantics.
- Updated Historical Data Summary UI:
  - Historical Era dropdown now includes `Custom`.
  - Added two-thumb inclusive month-year slider with live `MMM YYYY` endpoints.
  - First switch to Custom initializes from current preset span.
  - Switching away and back preserves prior custom range.
  - Custom controls respect compare read-only follower state.
- Updated compare parameter differences output:
  - Historical Era row now renders `Custom (MMM YYYY - MMM YYYY)` when applicable.
- Added/updated tests:
  - Server: historical data month-boundary filtering + custom summary query + MC custom-range run + route validation for missing/invalid custom range.
  - Client: compare lock/sync propagation for custom range, tracking stale behavior on custom-range updates, snapshot legacy defaults, and custom-era diff formatting.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated `docs/SPECS.md`, `docs/SCENARIOS.md`, `docs/DATA_MODEL.md`, `docs/API.md`, `docs/ARCHITECTURE.md`.
- No canonical doc impact required for `docs/PRD.md` (feature is within existing product scope).

2026-03-02 — Feature: Historical Era Custom Range v1.1 (Event Labels) | COMPLETE
- Added a broad historical event catalog for the custom month-year slider (`45` events, `1929-10` through `2024-01`) in `packages/client/src/lib/historicalEvents.ts`.
- Implemented exact-match month-year lookup (`getHistoricalEventLabel`) so labels only appear on explicitly mapped event months.
- Updated `HistoricalDataSummary` custom-range UI to render independent start/end event labels above the slider:
  - Left label tracks the start thumb month-year.
  - Right label tracks the end thumb month-year.
  - Non-event months show fallback text: `No mapped event`.
- Added unit tests for event lookup semantics and catalog integrity in `packages/client/src/lib/historicalEvents.test.ts`.
- Added feature update docs under `docs/features/historical-era-custom-range-v1-1/` with Delta From Baseline linkage.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`

Canonical docs impact:
- Updated `docs/SPECS.md` (Affordance #11a custom slider event-label behavior).
- Updated `docs/SCENARIOS.md` (Monte Carlo custom-range scenario now includes independent thumb event labels).
- No canonical doc impact required for `docs/DATA_MODEL.md`, `docs/API.md`, `docs/ARCHITECTURE.md`, or `docs/PRD.md` (no contract/model/architecture/product-scope changes).

2026-03-02 — Historical Era Event Labels fallback copy removal | COMPLETE
- Removed `No mapped event` fallback text from custom-range slider event labels.
- Updated `HistoricalDataSummary` to render event label text only when an exact month-year event exists.
- Updated docs to reflect fallback behavior:
  - `docs/SPECS.md` now states non-event months show no event label text.
  - `docs/features/historical-era-custom-range-v1-1/PLAN.md` and `ACCEPTANCE.md` aligned.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`

2026-03-02 — Historical Era Event Labels: slider bounce fix | COMPLETE
- Resolved slider vertical bounce caused by conditional event-label rendering.
- Refactored custom-range slider label layout to use absolute positioning in a fixed-height wrapper above the slider lane.
- Preserved previous thumb/track alignment by keeping the slider lane at its original geometry.

Validation run (all passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`

2026-03-02 — Snapshot/Bookmark guard: Custom Historical Era + legacy v6 load | COMPLETE
- Added explicit snapshot regression coverage for Custom Historical Era persistence (`selectedHistoricalEra=custom` + `customHistoricalRange`) through save/load.
- Added explicit v6 snapshot migration coverage to ensure defaults are backfilled (`customHistoricalRange=null`, `blockBootstrapEnabled=false`, `blockBootstrapLength=12`).
- Added explicit bookmark regression coverage for Custom Historical Era persistence through bookmark create/apply.
- Added explicit legacy v6 bookmark payload coverage by downgrading stored compressed snapshot payload to `schemaVersion=6` and validating successful load/backfill defaults.

Validation run (all passing):
- `npm run test -w @finapp/client`
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`

2026-03-02 — Historical Era Event Labels: decay timing polish | COMPLETE
- Implemented per-thumb decaying event label behavior in `HistoricalDataSummary`.
- Behavior now matches UX timing request:
  - Exact event match shows label immediately.
  - Leaving event month holds label for `250ms`.
  - Label then fades out over `100ms` using opacity transition.
- Added timer cancellation logic so fast thumb scrubbing does not queue stale fades.

Validation run (all passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm run test -w @finapp/client`

2026-03-02 — Hotfix: Monte Carlo mode hook-order crash in HistoricalDataSummary | COMPLETE
- Fixed React hook-order violation (`Rendered more hooks than during the previous render`) introduced by event-label decay hook placement.
- Moved decaying label hook invocations to a stable unconditional location in `HistoricalDataSummary` so hook count/order does not vary across loading/error/empty/ready render paths.
- Preserved decay timing behavior (250ms hold + 100ms fade) and exact-match event semantics.

Validation run (all passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm run test -w @finapp/client`

2026-03-02 — Feature: Historical Era Custom Range v1.2 (Event Snap) | COMPLETE
- Added magnetic event-month snapping helper in `packages/client/src/lib/historicalEvents.ts`:
  - `snapToHistoricalEventOrdinal(ordinal, { minOrdinal, maxOrdinal, thresholdMonths })`
  - Defaults to 1-month threshold and only considers in-range event months.
- Applied snapping to both custom-range slider thumbs in `HistoricalDataSummary` before start/end clamping.
- Added helper tests in `packages/client/src/lib/historicalEvents.test.ts` for:
  - near-event snapping,
  - out-of-range no-snap behavior.
- Added docs-first update wave: `docs/features/historical-era-custom-range-v1-2/{FEATURE,PLAN,ACCEPTANCE}.md`.
- Updated canonical spec (`docs/SPECS.md`) to document light magnetic snapping near event months.

Validation run (all passing):
- `npm run typecheck -w @finapp/client`
- `npm run lint -w @finapp/client`
- `npm run test -w @finapp/client`
- CHG-0010 complete: Monte Carlo monthly withdrawal summary cards now use cross-run aggregation.
  - Root cause confirmed: cards #35–#39 were derived from representative-path `result.rows`, causing high rerun variance.
  - Added optional MC payload field `withdrawalStatsReal` in shared contracts (`packages/shared/src/domain/simulation.ts`).
  - Server aggregation update (`packages/server/src/engine/monteCarlo.ts`): computes median/mean/stddev/p25/p75 of real monthly withdrawals across all simulations.
  - Client summary update (`packages/client/src/components/output/SummaryStatsBar.tsx`): in MC mode, uses `withdrawalStatsReal` for #35–#39 in both single and compare views; manual mode remains unchanged.
  - Tests updated:
    - `packages/server/tests/engine/monteCarlo.test.ts`
    - `packages/server/tests/routes/simulation.test.ts`
  - Canonical docs updated:
    - `docs/SPECS.md`
    - `docs/API.md`
    - `docs/DATA_MODEL.md`
  - Change lane docs/tracking:
    - `docs/changes/CHG-0010-mc-summary-stats-cross-run-aggregation/{CHANGE.md,ACCEPTANCE.md}`
    - `docs/changes/INDEX.md`
    - `TASKS.md` (`CHG-0010-T1`)

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- CHG-0010 refinement: monthly-withdrawal aggregation method corrected.
  - Replaced flattened run-month sample aggregation with month-aligned cross-run p50 series aggregation in `packages/server/src/engine/monteCarlo.ts`.
  - Rationale: flattened aggregation over-weighted outlier months/runs and produced unintuitive mean/stddev values.
  - New method now matches expected semantics used for MC reference-series reasoning:
    1) per-month p50 across runs,
    2) derive summary card metrics from that monthly p50 series.
  - Added/updated tests in `packages/server/tests/engine/monteCarlo.test.ts` for ordered quantiles and zero-withdrawal behavior.
  - Updated semantics text in `docs/SPECS.md` and `docs/changes/CHG-0010-mc-summary-stats-cross-run-aggregation/CHANGE.md`.

Validation rerun (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- RSU-T1..RSU-T5 complete: Returns source unification implemented (Manual/Historical source + run count) and command-bar simulation-type toggle removed.
  - Shared contracts updated with `ReturnSource` and optional `simulationRuns` (`packages/shared/src/constants/enums.ts`, `packages/shared/src/domain/simulation.ts`, `packages/shared/src/contracts/schemas.ts`).
  - Server route now normalizes effective run mode from source/runs/std-dev and returns normalized `configSnapshot` (`packages/server/src/routes/simulation.ts`).
  - Monte Carlo engine now supports both historical sampling and manual-assumption stochastic sampling at up to 10,000 runs (`packages/server/src/engine/monteCarlo.ts`).
  - Client sidebar now has unified `Returns` section with Manual/Historical toggle + simulation runs input, and source-specific detail panel (`packages/client/src/components/layout/Sidebar.tsx`, `packages/client/src/components/inputs/ReturnAssumptions.tsx`, `packages/client/src/components/inputs/HistoricalDataSummary.tsx`).
  - Command bar run flow now stores results by `response.simulationMode` to avoid stale mode mismatches (`packages/client/src/components/layout/CommandBar.tsx`).
  - Snapshot/store plumbing now persists + backfills `returnsSource` and `simulationRuns` for compatibility (`packages/client/src/store/useAppStore.ts`, `packages/client/src/store/snapshot.ts`).
  - Server fixtures/routes tests updated for explicit manual-path requests where required (`packages/server/tests/fixtures.ts`, `packages/server/tests/routes/simulation.test.ts`).

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
- MCPERF-T1 kickoff (2026-03-06): docs-first performance wave created at `docs/features/monte-carlo-parallel-performance/` with FEATURE/PLAN/ACCEPTANCE; implementing server-side Monte Carlo optimization + parallel throughput (no Rust in this wave).
- MCPERF-T2..MCPERF-T5 complete (2026-03-06): Monte Carlo parallel-performance wave shipped.
  - Engine optimization (`packages/server/src/engine/monteCarlo.ts`):
    - Removed full per-run path retention; now store compact run summaries and deterministically replay only selected representative run.
    - Percentile curves now sort each month distribution once per asset and derive all required quantiles from the same sorted array.
    - Run count clamp and seed handling preserved with deterministic `seedUsed` output.
  - Stress run-count parity (`packages/server/src/engine/stress.ts`):
    - Monte Carlo stress base/scenario runs now use clamped `config.simulationRuns` (`1..10000`) instead of fixed 1000.
  - Throughput parallelism:
    - Added process-level server clustering in `packages/server/src/server.ts` (configurable via `FINAPP_SERVER_WORKERS`; production default `availableParallelism()-1`; disabled in test runtime).
    - Increased compare and compare-stress client queue caps from 4 to 8 in `packages/client/src/components/layout/CommandBar.tsx` and `packages/client/src/components/output/StressTestPanel.tsx`.
  - Tests/docs:
    - Added stress MC run-count assertion in `packages/server/tests/engine/stress.test.ts`.
    - Canonical docs updated: `docs/API.md`, `docs/ARCHITECTURE.md`.

Validation run (all passing):
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run build`
