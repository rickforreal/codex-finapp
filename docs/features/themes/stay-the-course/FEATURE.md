# FEATURE: New Theme - "Stay The Course"

Add a new built-in dark theme named **Stay The Course** using the provided palette:

- `rgba(4, 242, 198, 1)` (`#04F2C6`)
- `rgba(4, 242, 174, 1)` (`#04F2AE`)
- `rgba(4, 140, 101, 1)` (`#048C65`)
- `rgba(4, 242, 155, 1)` (`#04F29B`)
- `rgba(12, 12, 12, 1)` (`#0C0C0C`)

Theme intent:
- dark foundation (`#0C0C0C`) with neon-teal accents
- high readability for finance dashboards and dense data tables
- consistent semantic behavior with the existing token model v2 and slot-driven styling
